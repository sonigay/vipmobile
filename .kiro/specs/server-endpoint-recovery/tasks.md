# Implementation Plan: Git Rollback & Router Recovery

## Overview

Git 커밋 `6725192670f37567eb230b44760881254b78ac86` (대규모 리팩토링 이전)으로 롤백한 후, 현재 작업한 라우터 파일들을 하나씩 복원하면서 정확한 엔드포인트와 Google Sheets 참조를 맞춰가며 수정합니다.

**핵심 전략:**
- Git 롤백으로 신뢰할 수 있는 원본 복구
- 현재 라우터 파일 백업 보존
- 하나씩 복원하며 검증
- **중요: server/index.js는 40000줄 넘는 원본 파일 (Git 롤백 완료됨)**
- **중요: server/index.js.current는 리팩토링 후 파일 (491줄, 참고용)**

---

## 🚨 중요: 올바른 작업 방법론

### ❌ 잘못된 접근 (Task 6-8에서 실수한 방법)
- 엔드포인트가 "존재하는지"만 확인
- 현재 라우터 파일만 읽고 완료 처리
- 백업 파일의 정확한 로직 확인 안 함

### ✅ 올바른 접근 (반드시 따라야 할 방법)

#### 1단계: 원본 로직 추출
```bash
# grepSearch로 server/index.js에서 엔드포인트 찾기
예: app.get('/api/stores'
예: app.post('/api/login'
```

#### 2단계: 원본 로직 상세 분석
- **시트 이름**: 어떤 시트를 사용하는가?
- **시트 범위**: A:P? A:AF? A:Z?
- **헤더 행 수**: slice(1)? slice(3)?
- **컬럼 인덱스**: row[15]는 몇 번째 열인가? (P열 = 15)
- **데이터 처리**: 필터링 조건, slice, map 등
- **응답 구조**: 어떤 필드를 반환하는가?

#### 3단계: 현재 라우터 파일과 비교
- 시트 이름이 같은가?
- 시트 범위가 같은가?
- 컬럼 인덱스가 정확한가?
- 데이터 처리 로직이 동일한가?
- 응답 구조가 같은가?

#### 4단계: 차이점 발견 시 수정
- 원본 로직으로 정확히 교체
- 주석으로 컬럼 설명 추가 (예: `row[15] // P열: 매장ID`)

#### 5단계: 비교 문서 작성
- `.kiro/specs/server-endpoint-recovery/{routerName}-comparison.md` 생성
- 차이점 상세 기록
- 수정 내역 문서화

---

## 📋 작업 체크리스트 (각 라우터마다 반복)

- [ ] grepSearch로 원본 엔드포인트 찾기
- [ ] readFile로 원본 로직 전체 읽기 (100-200줄 단위)
- [ ] 시트 이름, 범위, 컬럼 인덱스 확인
- [ ] 현재 라우터 파일 읽기
- [ ] 차이점 분석 문서 작성
- [ ] 차이점 발견 시 원본 로직으로 수정
- [ ] Task 완료 처리

---

## 🎯 우선순위 라우터 (프론트엔드 직접 연결)

### 최우선 (로그인 불가 시 앱 사용 불가)
1. ✅ **authRoutes.js** - POST /api/login (완료)
2. **storeRoutes.js** - GET /api/stores
3. **agentRoutes.js** - GET /api/agents

### 높은 우선순위 (주요 기능)
4. **teamRoutes.js** - GET /api/teams, GET /api/team-leaders
5. **salesRoutes.js** - GET /api/sales-data
6. **activationRoutes.js** - GET /api/activation-data/*
7. **modelRoutes.js** - GET /api/models

### 중간 우선순위
8. **coordinateRoutes.js** - POST /api/update-coordinates
9. **mapDisplayRoutes.js** - GET /api/map-display-option
10. **inventoryRecoveryRoutes.js** - GET /api/inventoryRecoveryAccess

### 낮은 우선순위
11. **memberRoutes.js** - 직영점 관련
12. **directStoreAdditionalRoutes.js** - 직영점 추가 기능
13. **onsaleRoutes.js** - 온세일 관리

---

## Tasks

  - [ ] 1.1 현재 routes 폴더 백업
    - server/routes 폴더를 server/routes.backup.broken으로 복사
    - 현재 작업 내용 보존
    
  - [ ] 1.2 현재 index.js 백업
    - server/index.js를 server/index.js.current로 복사
    - 현재 라우터 등록 코드 보존
    
  - [ ] 1.3 Git 롤백 실행
    - git checkout 6725192670f37567eb230b44760881254b78ac86 -- server/
    - 리팩토링 이전 상태로 복구
    - **중요: 이후 server/index.js는 절대 읽지 않음**
    
  - [ ] 1.4 서버 정상 작동 확인
    - npm start로 서버 시작
    - 브라우저에서 애플리케이션 테스트
    - 주요 기능 정상 작동 확인

- [ ] 2. 현재 라우터 모듈 분석
  - [x] 2.1 모든 라우터 모듈 파일 스캔 및 엔드포인트 추출
    - server/routes/ 디렉토리의 모든 .js 파일 읽기
    - router.get, router.post 등의 패턴으로 엔드포인트 추출
    - 각 라우터 모듈의 베이스 경로 식별 (index.js의 app.use 분석)
    - 추출 결과를 current-endpoints.json에 저장
    - _Requirements: 2.1, 2.2_

  - [x] 2.2 백업 파일과 현재 모듈 간 차이 분석
    - 백업 파일의 엔드포인트와 현재 엔드포인트 비교
    - 누락된 엔드포인트 목록 생성 (missing-endpoints.json)
    - 중복된 엔드포인트 목록 생성 (duplicate-endpoints.json)
    - 변경된 엔드포인트 목록 생성 (modified-endpoints.json)
    - 차이 분석 리포트 생성 (analysis-report.md)
    - _Requirements: 2.3, 2.4, 2.5_

  - [ ]* 2.3 엔드포인트 차이 식별 정확성 검증 (Property 6, 7)
    - **Property 6: 엔드포인트 차이 식별 정확성**
    - **Property 7: 중복 엔드포인트 검출 정확성**
    - **Validates: Requirements 2.3, 2.4, 2.5, 5.1**

- [ ] 3. 엔드포인트 매핑 전략 수립
  - [x] 3.1 라우터 매핑 규칙 정의
    - URL 패턴별 라우터 모듈 매핑 테이블 작성
    - 각 라우터 모듈의 책임 범위 명확화
    - 새로운 라우터 모듈 필요 여부 판단
    - 매핑 전략 문서 작성 (mapping-strategy.md)
    - _Requirements: 3.1, 3.2, 3.3_

  - [x] 3.2 베이스 경로 및 URL 패턴 표준화
    - 모든 라우터 모듈의 베이스 경로 정의
    - RESTful 원칙에 따른 URL 패턴 검증
    - 일관된 네이밍 규칙 적용 (소문자, 하이픈 구분)
    - 베이스 경로 충돌 검사
    - _Requirements: 3.4, 3.5_

  - [ ]* 3.3 URL 패턴 및 베이스 경로 검증 (Property 8, 9, 16)
    - **Property 8: RESTful URL 패턴 검증**
    - **Property 9: 베이스 경로 네이밍 일관성**
    - **Property 16: 베이스 경로 충돌 검사**
    - **Validates: Requirements 3.4, 3.5, 6.3, 9.3**

- [ ] 4. Checkpoint - 분석 결과 검토
  - 추출된 엔드포인트 목록 확인
  - 누락/중복/변경된 엔드포인트 검토
  - 매핑 전략 승인
  - 사용자에게 질문이 있으면 확인

- [ ] 5. 핵심 엔드포인트 복구 (Phase 1)
  - [x] 5.1 Health & Monitoring 엔드포인트 복구
    - healthRoutes.js에 누락된 엔드포인트 추가
    - GET /health, GET /, GET /api/version 복구
    - Shared Context 사용하도록 코드 변환
    - 에러 처리 및 로깅 추가
    - _Requirements: 4.1, 4.2, 4.3, 4.4_

  - [x] 5.2 Authentication 엔드포인트 복구
    - authRoutes.js에 누락된 엔드포인트 추가
    - POST /api/login, POST /api/verify-password, POST /api/verify-direct-store-password 복구
    - 비밀번호 검증 로직 정확히 이식
    - _Requirements: 4.1, 4.3, 4.4_

  - [x] 5.3 Logging & Cache 엔드포인트 복구
    - loggingRoutes.js에 POST /api/client-logs, POST /api/log-activity 추가
    - cacheRoutes.js에 GET /api/cache-status, POST /api/cache-refresh 추가
    - 캐시 관리 로직 복구
    - _Requirements: 4.1, 4.3, 4.4_

  - [ ]* 5.4 핵심 엔드포인트 복구 검증 (Property 10, 11, 12, 13)
    - **Property 10: 복구된 엔드포인트 미들웨어 보존**
    - **Property 11: Shared Context 사용 검증**
    - **Property 12: 에러 처리 포함 검증**
    - **Property 13: 엔드포인트 라우터 매핑 정확성**
    - **Validates: Requirements 4.2, 4.3, 4.4, 4.5, 7.1, 7.4**

- [ ] 6. 데이터 조회 엔드포인트 복구 (Phase 2) ⚠️ **전체 재검증 필요**
  - [x] 6.1 Team & Coordinate 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - teamRoutes.js: GET /api/teams, GET /api/team-leaders
    - coordinateRoutes.js: POST /api/update-coordinates, POST /api/update-sales-coordinates
    - 시트 이름, 범위, 컬럼 인덱스 정확히 확인
    - _Requirements: 4.1, 4.3, 4.4_

  - [x] 6.2 Store, Model, Agent 엔드포인트 복구 ⚠️ **진행 중 - 재검증**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - storeRoutes.js: GET /api/stores (최우선)
    - modelRoutes.js: GET /api/models
    - agentRoutes.js: GET /api/agents (최우선)
    - 캐싱 로직, 필터링 조건 정확히 확인
    - _Requirements: 4.1, 4.3, 4.4, 8.1, 8.2_

  - [x] 6.3 Map Display Options 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - mapDisplayRoutes.js: 5개 엔드포인트
    - GET /api/map-display-option, POST /api/map-display-option
    - POST /api/map-display-option/batch, GET /api/map-display-option/values
    - GET /api/map-display-option/users
    - _Requirements: 4.1, 4.3, 4.4_

  - [ ]* 6.4 데이터 조회 엔드포인트 캐싱 검증 (Property 20, 21)
    - **Property 20: 캐시 우선 조회**
    - **Property 21: Rate Limiter 적용 검증**
    - **Validates: Requirements 8.1, 8.2, 8.3**

- [ ] 7. 영업/판매 엔드포인트 복구 (Phase 3) ⚠️ **전체 재검증 필요**
  - [x] 7.1 Sales & Activation 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - salesRoutes.js: GET /api/sales-data, GET /api/sales-mode-access
    - activationRoutes.js: 4개 엔드포인트
    - GET /api/activation-data/current-month, GET /api/activation-data/previous-month
    - GET /api/activation-data/by-date, GET /api/activation-data/date-comparison/:date
    - _Requirements: 4.1, 4.3, 4.4_

  - [x] 7.2 Inventory Recovery 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - inventoryRecoveryRoutes.js: GET /api/inventoryRecoveryAccess
    - 재고회수 모드 접근 권한 검증 로직 복구
    - _Requirements: 4.1, 4.3, 4.4, 4.5_

  - [ ]* 7.3 영업/판매 엔드포인트 통합 테스트
    - 영업 데이터 조회 테스트
    - 개통 실적 조회 테스트
    - 날짜별 비교 테스트
    - _Requirements: 10.1, 10.2_

- [ ] 8. 직영점 관련 엔드포인트 복구 (Phase 4) ⚠️ **전체 재검증 필요**
  - [x] 8.1 Member Management 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - memberRoutes.js: 11개 엔드포인트
    - POST /api/member/login, GET /api/member/queue/all
    - GET /api/member/queue, POST /api/member/queue
    - PUT /api/member/queue/:id, DELETE /api/member/queue/:id
    - GET /api/member/board, GET /api/member/board/:id
    - POST /api/member/board, PUT /api/member/board/:id, DELETE /api/member/board/:id
    - _Requirements: 4.1, 4.3, 4.4_

  - [x] 8.2 Direct Store Additional 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - directStoreAdditionalRoutes.js: 6개 엔드포인트
    - GET /api/direct/drive-monitoring
    - GET /api/direct/pre-approval-mark/:storeName, POST /api/direct/pre-approval-mark
    - GET /api/direct/store-image/:storeName, POST /api/direct/store-image
    - POST /api/direct/store-image/upload
    - _Requirements: 4.1, 4.3, 4.4_

  - [x] 8.3 Onsale Management 엔드포인트 복구 ⚠️ **재검증 필요**
    - ❌ 이전에 엔드포인트 존재만 확인하고 완료 처리 (잘못됨)
    - ✅ 올바른 방법: 원본 index.js에서 로직 추출 → 비교 → 수정
    - onsaleRoutes.js: POST /api/onsale/activation-info/:sheetId/:rowIndex/complete
    - 개통 완료 처리 로직 복구
    - _Requirements: 4.1, 4.3, 4.4_

  - [ ]* 8.4 직영점 엔드포인트 통합 테스트
    - 고객 대기열 CRUD 테스트
    - 게시판 CRUD 테스트
    - 매장 사진 업로드 테스트
    - _Requirements: 10.1, 10.2_

- [x] 9. Checkpoint - 엔드포인트 복구 완료 확인
  - 모든 누락된 엔드포인트가 복구되었는지 확인
  - 각 라우터 모듈이 정상 로드되는지 확인
  - 서버 시작 시 에러가 없는지 확인
  - 사용자에게 질문이 있으면 확인

- [x] 10. 중복 엔드포인트 제거 및 최적화
  - [x] 10.1 중복 엔드포인트 식별 및 제거
    - duplicate-endpoints.json 기반으로 중복 검사
    - 동일한 로직을 가진 중복은 자동 제거
    - 다른 로직을 가진 중복은 수동 검토 요청 (경고 출력)
    - 제거된 엔드포인트 문서화 (removed-duplicates.md)
    - _Requirements: 5.1, 5.2, 5.3, 5.4_

  - [x] 10.2 중복 제거 후 라우팅 테이블 검증
    - 모든 엔드포인트가 유일한지 확인
    - 라우팅 충돌이 없는지 확인
    - 검증 결과 리포트 생성
    - _Requirements: 5.5_

  - [ ]* 10.3 중복 제거 검증 (Property 14)
    - **Property 14: 중복 제거 후 유일성 보장**
    - **Validates: Requirements 5.5**

- [x] 11. 에러 처리 및 미들웨어 표준화
  - [x] 11.1 모든 엔드포인트에 에러 처리 추가
    - try-catch 블록으로 모든 비동기 작업 감싸기
    - 적절한 HTTP 상태 코드 반환 (400번대, 500번대)
    - 구조화된 에러 응답 형식 적용 ({ error, details })
    - _Requirements: 7.1, 7.2, 7.3_

  - [x] 11.2 에러 로깅 표준화
    - 모든 에러를 콘솔에 로깅 (형식: ❌ [카테고리] 에러 설명)
    - 중요한 에러는 Discord로 알림 (선택적)
    - 에러 로깅 유틸리티 함수 작성
    - _Requirements: 7.5_

  - [ ]* 11.3 에러 처리 검증 (Property 17, 18, 19)
    - **Property 17: HTTP 상태 코드 적절성**
    - **Property 18: 에러 응답 구조 일관성**
    - **Property 19: 에러 로깅 수행 검증**
    - **Validates: Requirements 7.2, 7.3, 7.5**

- [x] 12. 캐싱 및 Rate Limiting 최적화
  - [x] 12.1 캐싱 전략 적용
    - Google Sheets API 호출 전 캐시 확인 로직 추가
    - 엔드포인트별 적절한 TTL 설정 (기본 5분)
    - 캐시 키 네이밍 규칙 표준화
    - _Requirements: 8.1, 8.2, 8.5_

  - [x] 12.2 Rate Limiting 적용
    - 모든 Google Sheets API 호출에 rateLimiter.execute() 적용
    - Rate Limit 오류 시 지수 백오프 재시도 로직 추가
    - 재시도 횟수 제한 (최대 5회)
    - _Requirements: 8.3, 8.4_

  - [ ]* 12.3 캐싱 및 Rate Limiting 검증 (Property 22)
    - **Property 22: Rate Limit 재시도 로직**
    - **Validates: Requirements 8.4**

- [x] 13. 라우터 등록 순서 최적화
  - [x] 13.1 라우터 등록 순서 재정렬
    - 구체적인 경로를 먼저 등록 (예: /api/stores/detail)
    - 파라미터 경로를 나중에 등록 (예: /api/stores/:id)
    - 와일드카드 경로를 마지막에 등록 (예: /api/*)
    - 라우터 등록 순서 문서화 (router-order.md)
    - _Requirements: 9.1, 9.2, 9.4_

  - [x] 13.2 라우터 등록 에러 처리
    - 라우터 로딩 실패 시 에러 로깅
    - 실패한 라우터를 건너뛰고 계속 진행
    - 서버 시작 시 등록된 라우터 목록 출력
    - _Requirements: 9.4_

  - [ ]* 13.3 라우터 등록 순서 검증 (Property 15)
    - **Property 15: 라우터 등록 순서 규칙 준수**
    - **Validates: Requirements 6.4, 9.1, 9.2**

- [x] 14. Google Sheets 참조 검증 및 수정
  - [x] 14.1 모든 Google Sheets 참조 추출
    - 모든 라우터 모듈에서 시트 이름과 범위 추출
    - sheets-references.json 파일 생성
    - 하드코딩된 시트 이름 목록 작성
    - _Requirements: 추가 이슈 해결_

  - [x] 14.2 실제 Google Sheets 구조와 비교
    - 실제 스프레드시트의 시트 이름 확인
    - 코드의 시트 이름과 비교하여 불일치 목록 작성
    - 열 범위가 실제 데이터 구조와 맞는지 확인
    - _Requirements: 추가 이슈 해결_

  - [x] 14.3 잘못된 시트 참조 수정
    - appUpdateRoutes.js의 시트 이름 및 범위 수정
    - policyRoutes.js 및 policyNoticeRoutes.js의 시트 참조 수정
    - 담당자 정보 및 정책 목록 조회 로직 수정
    - 모든 수정 사항 문서화 (sheets-fixes.md)
    - _Requirements: 추가 이슈 해결_

- [x] 15. Checkpoint - 최적화 완료 확인
  - 모든 엔드포인트에 에러 처리가 적용되었는지 확인
  - 캐싱 및 Rate Limiting이 올바르게 작동하는지 확인
  - 라우터 등록 순서가 최적화되었는지 확인
  - Google Sheets 참조가 정확한지 확인
  - 사용자에게 질문이 있으면 확인

- [x] 16. 통합 테스트 및 검증
  - [x] 16.1 엔드포인트별 기본 테스트 작성
    - 각 엔드포인트에 대한 성공 케이스 테스트
    - 각 엔드포인트에 대한 실패 케이스 테스트 (최소 1개)
    - supertest를 사용한 HTTP 요청 테스트
    - _Requirements: 10.1, 10.2_

  - [x] 16.2 테스트 실행 및 결과 분석
    - npm test 실행하여 모든 테스트 수행
    - 실패한 테스트 분석 및 수정
    - 테스트 커버리지 확인 (목표: 80% 이상)
    - 테스트 결과 리포트 생성 (test-report.md)
    - _Requirements: 10.3, 10.4_

  - [ ]* 16.3 테스트 커버리지 검증 (Property 23, 24)
    - **Property 23: 테스트 커버리지 완전성**
    - **Property 24: 테스트 실패 로깅 충분성**
    - **Validates: Requirements 10.2, 10.3**

- [x] 17. 수동 검증 및 프로덕션 준비
  - [x] 17.1 서버 시작 및 기본 동작 확인
    - npm start로 서버 시작
    - 서버가 에러 없이 시작되는지 확인
    - 모든 라우터 모듈이 정상 등록되는지 확인
    - 콘솔 로그에서 에러 메시지 확인
    - _Requirements: 10.5_

  - [x] 17.2 주요 엔드포인트 수동 테스트
    - curl 또는 Postman으로 주요 엔드포인트 테스트
    - GET /health, GET /api/teams, GET /api/stores 테스트
    - POST /api/login 테스트
    - 캐싱이 올바르게 작동하는지 확인 (두 번째 요청이 빠른지)
    - _Requirements: 10.5_

  - [x] 17.3 프론트엔드 통합 테스트
    - 프론트엔드 애플리케이션 시작
    - 모든 모드(재고, 영업, 직영점 등)가 정상 작동하는지 확인
    - 지도 표시, 데이터 조회, 필터링 등 주요 기능 테스트
    - 에러가 발생하지 않는지 확인
    - _Requirements: 10.5_

  - [x] 17.4 성능 및 안정성 확인
    - 응답 시간이 리팩토링 이전과 비슷한지 확인
    - 메모리 사용량이 정상 범위인지 확인
    - 동시 요청 처리가 정상적인지 확인
    - Discord 로깅이 작동하는지 확인 (설정된 경우)
    - _Requirements: 10.5_

- [-] 18. 최종 문서화 및 배포 준비
  - [ ] 18.1 복구 작업 문서화
    - 복구된 엔드포인트 목록 정리
    - 제거된 중복 엔드포인트 목록 정리
    - 수정된 Google Sheets 참조 목록 정리
    - 최종 복구 리포트 작성 (recovery-report.md)
    - _Requirements: 5.3, 9.5_

  - [ ] 18.2 라우터 모듈 주석 및 문서 추가
    - 각 라우터 모듈에 명확한 주석 추가
    - 각 엔드포인트의 목적과 사용법 설명
    - 베이스 경로 및 책임 범위 문서화
    - _Requirements: 6.5_

  - [-] 18.3 배포 체크리스트 작성
    - 프로덕션 배포 전 확인 사항 목록 작성
    - 환경 변수 확인 항목 추가
    - 롤백 계획 수립
    - 배포 후 모니터링 계획 수립
    - _Requirements: 10.5_

- [x] 19. Final Checkpoint - 배포 준비 완료
  - 모든 테스트가 통과했는지 확인
  - 수동 검증이 완료되었는지 확인
  - 문서화가 완료되었는지 확인
  - 배포 체크리스트 검토
  - 사용자에게 최종 승인 요청

## Notes

- 태스크에 `*` 표시가 있는 것은 선택적 태스크입니다 (빠른 MVP를 위해 건너뛸 수 있음)
- 각 태스크는 특정 요구사항을 참조하여 추적 가능성을 보장합니다
- Checkpoint는 점진적 검증을 보장합니다
- Property 테스트는 보편적 정확성 속성을 검증합니다
- 단위 테스트는 특정 예제 및 엣지 케이스를 검증합니다
- 통합 테스트는 엔드투엔드 흐름을 검증합니다
