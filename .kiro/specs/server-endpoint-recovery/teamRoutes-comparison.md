# teamRoutes.js 로직 비교 분석

## 분석 일시
2025-01-25

## 비교 대상
- **원본**: `server/index.js` (Git 롤백 버전, 222-330줄)
- **현재**: `server/routes/teamRoutes.js`

---

## ⚠️ 비교 결과: 중요한 차이점 발견

### GET /api/teams

#### 시트 참조
- ✅ **시트 이름**: 동일
  - `대리점아이디관리`

#### 시트 범위
- 🔴 **차이 발견**:
  - 원본: `A:R` (R열까지)
  - 현재: `A:P` (P열까지)
  - **영향**: R열(17)의 정책모드권한레벨을 읽지 못함!

#### 헤더 행 수
- ✅ **동일**:
  - `slice(1)` (첫 1행 제외)

#### 컬럼 인덱스
- 🔴 **차이 발견**:
  - 원본: `row[17]` (R열 - 정책모드권한레벨)
  - 현재: `row[15]` (P열 - 잘못된 컬럼)
  - **영향**: 잘못된 컬럼을 읽어서 팀 목록이 부정확함!

#### 필터링 로직
- 🔴 **차이 발견**:
  - 원본: `/^[A-Z]{2}$/` (모든 알파벳 2자 조합)
  - 현재: `['AA', 'BB', 'CC', 'DD', 'EE', 'FF'].includes()` (하드코딩)
  - **영향**: GG, HH 등 다른 팀이 있으면 누락됨!

#### 하드코딩
- 🔴 **문제 발견**:
  - 현재: '홍남옥' 하드코딩 추가
  - 원본: 하드코딩 없음
  - **영향**: 불필요한 하드코딩

#### 응답 구조
- 🔴 **차이 발견**:
  - 원본: `teams` 배열 직접 반환
  - 현재: `{ success: true, teams: [...] }` 객체 반환
  - **영향**: 프론트엔드 호환성 문제 가능

---

### GET /api/team-leaders

#### 시트 범위
- ✅ **동일**: `A:R`

#### 컬럼 인덱스
- ✅ **동일**: `row[17]` (R열)

#### 필터링 로직
- ✅ **동일**: `/^[A-Z]{2}$/`

#### 응답 구조
- ✅ **동일**: 배열 직접 반환

#### 에러 처리
- ✅ **동일**: 시트 없을 때 빈 배열 반환

---

## 🚨 치명적 문제

### GET /api/teams의 문제점

1. **잘못된 시트 범위**: `A:P` → `A:R`로 변경 필요
2. **잘못된 컬럼 인덱스**: `row[15]` → `row[17]`로 변경 필요
3. **하드코딩된 필터**: `['AA', 'BB', ...]` → `/^[A-Z]{2}$/`로 변경 필요
4. **불필요한 하드코딩**: '홍남옥' 제거 필요
5. **응답 구조 변경**: `{ success, teams }` → `teams` 배열로 변경 필요

---

## 🎯 수정 필요 사항

### GET /api/teams 수정
```javascript
// 변경 전
range: '대리점아이디관리!A:P'
code: row[15], // P열
.filter(team => ['AA', 'BB', 'CC', 'DD', 'EE', 'FF'].includes(team.code));
teams.push({ code: '홍남옥', name: '홍남옥' });
res.json({ success: true, teams });

// 변경 후
range: '대리점아이디관리!A:R'
code: row[17], // R열
.filter(team => team.code && team.code.length === 2 && /^[A-Z]{2}$/.test(team.code));
// 홍남옥 하드코딩 제거
res.json(teams);
```

---

## 📊 검증 완료

- ❌ 시트 범위 확인 - **수정 필요**
- ❌ 컬럼 인덱스 확인 - **수정 필요**
- ❌ 필터링 로직 확인 - **수정 필요**
- ❌ 하드코딩 확인 - **제거 필요**
- ❌ 응답 구조 확인 - **수정 필요**

**teamRoutes.js는 GET /api/teams 엔드포인트 수정 필요!**
**GET /api/team-leaders는 정확함!**
