# VIP Map Application 개발자 가이드

## 📋 목차

1. [시작하기](#시작하기)
2. [로컬 개발 환경 설정](#로컬-개발-환경-설정)
3. [API URL 설정](#api-url-설정)
4. [프로젝트 구조 이해](#프로젝트-구조-이해)
5. [개발 워크플로우](#개발-워크플로우)
6. [테스트 실행](#테스트-실행)
7. [트러블슈팅](#트러블슈팅)
8. [코딩 가이드라인](#코딩-가이드라인)
9. [배포 프로세스](#배포-프로세스)
10. [추가 리소스](#추가-리소스)

---

## 시작하기

### 환영합니다! 👋

VIP Map Application 개발팀에 오신 것을 환영합니다. 이 가이드는 새로운 개발자가 프로젝트에 빠르게 적응하고 기여할 수 있도록 돕기 위해 작성되었습니다.

### 프로젝트 개요

**VIP Map Application**은 휴대폰 재고 관리, 판매 추적, 매장 운영을 위한 종합 웹 애플리케이션입니다.

**주요 기능**:
- 🗺️ OpenStreetMap 기반 재고 시각화
- 📊 실시간 재고 관리 및 할당
- 💰 판매 추적 및 정산 워크플로우
- 📱 20개 이상의 운영 모드 (재고, 정산, 검수, 정책 등)
- 🔐 역할 기반 접근 제어 (대리점, 매장, 관리자)
- 📲 Android 동반 앱 (SMS 관리)

**기술 스택**:
- **Frontend**: React 18.2, Material-UI, Leaflet, Chart.js
- **Backend**: Node.js, Express, Google Sheets API
- **Android**: Kotlin
- **배포**: Vercel (Frontend), Cloudtype (Backend)

### 필수 요구사항

개발을 시작하기 전에 다음 도구들이 설치되어 있어야 합니다:


#### 소프트웨어 요구사항

| 도구 | 버전 | 필수 | 설치 링크 |
|------|------|------|-----------|
| **Node.js** | 22.x | ✅ | [nodejs.org](https://nodejs.org/) |
| **npm** | 10.x+ | ✅ | Node.js와 함께 설치됨 |
| **Git** | 최신 | ✅ | [git-scm.com](https://git-scm.com/) |
| **Android Studio** | Arctic Fox+ | ❌ (Android 개발 시) | [developer.android.com](https://developer.android.com/studio) |
| **VS Code** | 최신 | 권장 | [code.visualstudio.com](https://code.visualstudio.com/) |

#### 권장 VS Code 확장

- **ESLint**: JavaScript/React 린팅
- **Prettier**: 코드 포맷팅
- **ES7+ React/Redux/React-Native snippets**: React 스니펫
- **GitLens**: Git 히스토리 및 블레임
- **Thunder Client**: API 테스트 (Postman 대체)

### 저장소 클론

```bash
# HTTPS
git clone https://github.com/sonigay/vipmobile.git

# SSH (권장)
git clone git@github.com:sonigay/vipmobile.git

# 프로젝트 디렉토리로 이동
cd vipmobile
```

---

## 로컬 개발 환경 설정

### 1단계: Node.js 버전 확인

프로젝트는 **Node.js 22.x**를 사용합니다. `.nvmrc` 파일에 명시되어 있습니다.

```bash
# Node.js 버전 확인
node --version
# 출력: v22.x.x

# npm 버전 확인
npm --version
# 출력: 10.x.x
```

**nvm 사용 시** (권장):
```bash
# .nvmrc 파일의 버전 사용
nvm use

# 또는 직접 설치
nvm install 22
nvm use 22
```

### 2단계: Frontend 의존성 설치


프로젝트 루트 디렉토리에서:

```bash
# 의존성 설치
npm install

# 설치 확인
npm list --depth=0
```

**예상 설치 시간**: 2-5분 (네트워크 속도에 따라)

**주요 의존성**:
- `react`, `react-dom`: UI 라이브러리
- `@mui/material`: Material-UI 컴포넌트
- `leaflet`, `react-leaflet`: 지도 라이브러리
- `axios`: HTTP 클라이언트
- `chart.js`, `recharts`: 차트 라이브러리

### 3단계: Backend 의존성 설치

```bash
# server 디렉토리로 이동
cd server

# 의존성 설치
npm install

# 프로젝트 루트로 돌아가기
cd ..
```

**주요 의존성**:
- `express`: 웹 서버 프레임워크
- `googleapis`: Google Sheets API
- `discord.js`: Discord 로깅
- `multer`: 파일 업로드
- `node-cron`: 스케줄링

### 4단계: 환경 변수 설정

#### Frontend 환경 변수

프로젝트 루트에 `.env` 파일을 생성합니다:

```bash
# .env
REACT_APP_API_URL=http://localhost:4000
REACT_APP_ENV=development
REACT_APP_LOGGING_ENABLED=true
REACT_APP_PUSH_NOTIFICATIONS_ENABLED=false
REACT_APP_DISABLE_PRICE_CACHE=true
```

#### Backend 환경 변수

`server/.env` 파일을 생성합니다:

```bash
# server/.env
PORT=4000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Google Sheets (팀에서 제공받아야 함)
SHEET_ID=your_google_sheet_id
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----"

# Discord (선택사항)
DISCORD_LOGGING_ENABLED=false
DISCORD_BOT_TOKEN=your_discord_bot_token
DISCORD_CHANNEL_ID=your_discord_channel_id

# Kakao Maps (선택사항)
KAKAO_API_KEY=your_kakao_api_key
```

⚠️ **중요**: 
- `.env` 파일은 Git에 커밋하지 마세요 (`.gitignore`에 포함됨)
- Google Sheets 인증 정보는 팀 리더에게 요청하세요
- 로컬 개발 시 Discord 로깅은 비활성화하는 것을 권장합니다


### 5단계: 개발 서버 실행

#### 방법 1: 두 개의 터미널 사용 (권장)

**터미널 1 - Backend 서버**:
```bash
cd server
npm run dev
```

**출력 예시**:
```
✅ [CORS] 설정 로드 완료
📋 [CORS] 허용된 오리진 목록:
  - http://localhost:3000
  - http://127.0.0.1:3000
🔧 [CORS] 설정 출처: environment
🚀 Server is running on port 4000
```

**터미널 2 - Frontend 개발 서버**:
```bash
npm start
```

**출력 예시**:
```
Compiled successfully!

You can now view vipmobile in the browser.

  Local:            http://localhost:3000
  On Your Network:  http://192.168.0.10:3000
```

#### 방법 2: PM2 사용 (선택사항)

PM2를 사용하면 백그라운드에서 서버를 실행할 수 있습니다:

```bash
# PM2 설치 (전역)
npm install -g pm2

# Backend 서버 시작
pm2 start ecosystem.config.js

# 로그 확인
pm2 logs

# 서버 중지
pm2 stop all
```

### 6단계: 애플리케이션 접속

브라우저에서 다음 URL로 접속:
```
http://localhost:3000
```

**첫 화면**: 로그인 페이지가 표시됩니다.

**테스트 계정**: 팀 리더에게 테스트 계정 정보를 요청하세요.

### 7단계: 개발 환경 검증

#### Frontend 검증

1. **브라우저 개발자 도구 열기** (F12)
2. **Console 탭 확인**:
   - CORS 오류가 없어야 함
   - API 연결 성공 메시지 확인
3. **Network 탭 확인**:
   - API 요청이 `http://localhost:4000`로 전송되는지 확인
   - 응답 상태 코드가 200 OK인지 확인

#### Backend 검증

터미널에서 API 테스트:

```bash
# 헬스 체크
curl http://localhost:4000/health

# 예상 응답
{"status":"ok","timestamp":"2024-01-15T10:30:00.000Z"}

# CORS 테스트
curl -H "Origin: http://localhost:3000" \
     -H "Access-Control-Request-Method: GET" \
     -X OPTIONS \
     --verbose \
     http://localhost:4000/api/stores
```

**성공 지표**:
- ✅ Frontend가 http://localhost:3000에서 실행 중
- ✅ Backend가 http://localhost:4000에서 실행 중
- ✅ Console에 CORS 오류 없음
- ✅ API 요청이 정상적으로 응답

---

## API URL 설정

### 중앙화된 API URL 관리

VIP Map Application은 **단일 진실 공급원(Single Source of Truth)** 원칙을 따라 모든 API URL을 중앙에서 관리합니다.


### API URL의 단일 진실 공급원: `src/api.js`

모든 API 요청은 `src/api.js`에서 export된 `API_BASE_URL`을 사용합니다:

```javascript
// src/api.js
export const API_BASE_URL = process.env.REACT_APP_API_URL || 'https://vipmobile-backend.cloudtype.app';
```

**중요 원칙**:
- ✅ **항상** `src/api.js`에서 `API_BASE_URL`을 import하여 사용
- ❌ **절대** 컴포넌트나 유틸리티에서 URL을 하드코딩하지 않음
- ❌ **절대** 각 파일에서 `process.env.REACT_APP_API_URL`을 직접 사용하지 않음

### 올바른 사용 예시

```javascript
// ✅ 올바른 방법
import { API_BASE_URL } from '../api';

const fetchStores = async () => {
  const response = await fetch(`${API_BASE_URL}/api/stores`);
  return response.json();
};
```

```javascript
// ❌ 잘못된 방법 - 하드코딩
const fetchStores = async () => {
  const response = await fetch('https://vipmobile-backend.cloudtype.app/api/stores');
  return response.json();
};
```

```javascript
// ❌ 잘못된 방법 - 각 파일에서 환경 변수 직접 사용
const API_URL = process.env.REACT_APP_API_URL || 'https://vipmobile-backend.cloudtype.app';
```

### 환경별 API URL 설정

#### 로컬 개발
```bash
# .env
REACT_APP_API_URL=http://localhost:4000
```

#### 프로덕션 (Vercel)
```bash
# Vercel 환경 변수
REACT_APP_API_URL=https://vipmobile-backend.cloudtype.app
```

#### 하이브리드 (로컬 Frontend + 프로덕션 Backend)
```bash
# .env
REACT_APP_API_URL=https://vipmobile-backend.cloudtype.app
```

⚠️ **주의**: 환경 변수 변경 후에는 반드시 개발 서버를 재시작하거나 재빌드해야 합니다.

### CORS 설정 이해

Backend는 `ALLOWED_ORIGINS` 환경 변수로 허용된 오리진을 관리합니다:

```bash
# server/.env
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

**CORS 오류 발생 시**:
1. Backend 로그에서 요청 오리진 확인
2. `ALLOWED_ORIGINS`에 해당 오리진 추가
3. Backend 서버 재시작

**자세한 내용**: [환경 변수 설정 가이드](.kiro/specs/api-url-centralization/ENV_SETUP.md)

---

## 프로젝트 구조 이해

### 디렉토리 구조 개요

```
vipmobile/
├── src/                    # Frontend 소스 코드
│   ├── components/         # React 컴포넌트
│   ├── config/            # 설정 파일
│   ├── utils/             # 유틸리티 함수
│   ├── hooks/             # Custom React Hooks
│   ├── theme/             # 테마 설정
│   ├── App.js             # 메인 애플리케이션
│   ├── api.js             # API 클라이언트 (중요!)
│   └── index.js           # React 진입점
├── server/                # Backend 소스 코드
│   ├── __tests__/         # 테스트 파일
│   ├── index.js           # Express 서버
│   ├── corsMiddleware.js  # CORS 설정
│   ├── *Routes.js         # API 라우트 모듈
│   └── *Manager.js        # 데이터 관리자
├── android-app/           # Android 앱
├── docs/                  # 문서
├── public/                # 정적 파일
└── scripts/               # 빌드 스크립트
```


### 핵심 파일 설명

#### Frontend 핵심 파일

**`src/App.js`**
- 메인 애플리케이션 컴포넌트
- 라우팅 및 전역 상태 관리
- 모드 선택 및 권한 제어
- localStorage를 통한 상태 지속성

**`src/api.js`**
- **가장 중요한 파일!**
- API URL의 단일 진실 공급원
- API 클라이언트 및 캐싱 유틸리티
- 모든 컴포넌트가 이 파일에서 `API_BASE_URL`을 import

**`src/config/modeConfig.js`**
- 20개 이상의 운영 모드 정의
- 모드별 메타데이터 (제목, 색상, 아이콘, 권한)
- 권한 기반 접근 제어 설정

**`src/components/`**
- 각 운영 모드별 컴포넌트 (예: `InventoryMode.js`, `PolicyMode.js`)
- 공통 UI 컴포넌트 (예: `Map.js`, `Header.js`)
- 기능별 하위 디렉토리 (예: `budget/`, `direct/`, `policy/`)

**`src/utils/`**
- 재사용 가능한 유틸리티 함수
- API 호출 함수 (`api.js`)
- 비즈니스 로직 (예: `directStoreCalculationEngine.js`)
- 로깅 유틸리티 (`logger.js`, `remoteLogger.js`)

#### Backend 핵심 파일

**`server/index.js`**
- Express 서버 메인 파일
- API 라우트 정의
- Google Sheets 통합
- 미들웨어 설정

**`server/corsMiddleware.js` & `server/corsConfigManager.js`**
- CORS 설정 관리
- 동적 오리진 허용
- 환경 변수 기반 설정

**`server/*Routes.js`**
- 기능별 API 라우트 모듈
- 예: `directRoutes.js`, `policyTableRoutes.js`, `meetingRoutes.js`

**`server/*Manager.js`**
- 데이터 관리 클래스
- 예: `UserSheetManager.js`, `PhoneklDataManager.js`

### 모드 시스템 이해

VIP Map Application의 핵심은 **모드 시스템**입니다. 각 모드는 특정 운영 기능을 제공합니다.

#### 주요 모드 목록

| 모드 | 파일 | 설명 |
|------|------|------|
| **재고 모드** | `InventoryMode.js` | 재고 관리 및 할당 |
| **정산 모드** | `SettlementMode.js` | 재무 정산 워크플로우 |
| **검수 모드** | `InspectionMode.js` | 품질 검수 |
| **정책 모드** | `PolicyMode.js` | 정책 문서 관리 |
| **회의 모드** | `MeetingMode.js` | 회의 조율 |
| **예산 모드** | `BudgetMode.js` | 예산 추적 |
| **직영점 모드** | `DirectStoreMode.js` | 직영점 운영 |
| **OB 관리** | `ObManagementMode.js` | 아웃바운드 콜 관리 |
| **SMS 관리** | `SmsManagementMode.js` | SMS 자동화 |

#### 모드 추가 방법

새로운 모드를 추가하려면:

1. **`src/config/modeConfig.js`에 모드 정의 추가**:
```javascript
export const modes = {
  // ... 기존 모드들
  newMode: {
    title: '새 모드',
    color: '#FF5722',
    icon: '🆕',
    permissions: ['O', 'M'], // 허용된 역할
    description: '새로운 기능 설명'
  }
};
```

2. **`src/components/NewMode.js` 컴포넌트 생성**:
```javascript
import React from 'react';
import { API_BASE_URL } from '../api';

const NewMode = () => {
  // 컴포넌트 로직
  return <div>새 모드 UI</div>;
};

export default NewMode;
```

3. **`src/App.js`에서 모드 라우팅 추가**:
```javascript
import NewMode from './components/NewMode';

// render 함수 내부
{currentMode === 'newMode' && <NewMode />}
```

### 권한 시스템

권한은 Google Sheets의 `대리점아이디관리` 시트에서 관리됩니다:

- **O (Owner)**: 관리자, 모든 권한
- **M (Manager)**: 매니저, 대부분의 권한
- **S (Staff)**: 직원, 제한된 권한

각 모드는 `modeConfig.js`에서 허용된 역할을 정의합니다.

---

## 개발 워크플로우

### 일반적인 개발 프로세스


#### 1. 이슈 확인 및 브랜치 생성

```bash
# 최신 코드 가져오기
git checkout main
git pull origin main

# 기능 브랜치 생성
git checkout -b feature/new-feature-name

# 또는 버그 수정
git checkout -b fix/bug-description
```

**브랜치 명명 규칙**:
- `feature/`: 새로운 기능
- `fix/`: 버그 수정
- `refactor/`: 코드 리팩토링
- `docs/`: 문서 업데이트
- `test/`: 테스트 추가/수정

#### 2. 코드 작성

**Frontend 개발**:
```bash
# 개발 서버 실행 (자동 리로드)
npm start

# 코드 수정
# src/components/YourComponent.js
```

**Backend 개발**:
```bash
# 개발 서버 실행 (nodemon으로 자동 재시작)
cd server
npm run dev

# 코드 수정
# server/yourRoute.js
```

#### 3. 코드 스타일 확인

```bash
# ESLint 실행 (Frontend)
npm run lint

# 자동 수정
npm run lint:fix
```

#### 4. 테스트 실행

```bash
# Frontend 테스트
npm test

# Backend 테스트
cd server
npm test

# 특정 테스트 파일만 실행
npm test -- cors.test.js

# 커버리지 확인
npm run test:coverage
```

#### 5. 커밋 및 푸시

```bash
# 변경사항 확인
git status
git diff

# 스테이징
git add .

# 커밋 (의미 있는 메시지 작성)
git commit -m "feat: Add new inventory filter feature"

# 푸시
git push origin feature/new-feature-name
```

**커밋 메시지 규칙**:
- `feat:` - 새로운 기능
- `fix:` - 버그 수정
- `refactor:` - 코드 리팩토링
- `docs:` - 문서 변경
- `test:` - 테스트 추가/수정
- `style:` - 코드 포맷팅
- `chore:` - 빌드/설정 변경

#### 6. Pull Request 생성

1. GitHub에서 Pull Request 생성
2. 제목과 설명 작성
3. 리뷰어 지정
4. 테스트 통과 확인
5. 리뷰 피드백 반영
6. 승인 후 머지

### Hot Reload 및 디버깅

#### Frontend Hot Reload

React 개발 서버는 자동으로 Hot Reload를 지원합니다:
- 파일 저장 시 자동으로 브라우저 새로고침
- 대부분의 경우 상태 유지 (Fast Refresh)

#### Backend 자동 재시작

`nodemon`을 사용하여 파일 변경 시 자동 재시작:

```bash
cd server
npm run dev
```

#### 브라우저 디버깅

**Chrome DevTools 사용**:
1. F12로 개발자 도구 열기
2. **Sources** 탭에서 브레이크포인트 설정
3. **Console** 탭에서 로그 확인
4. **Network** 탭에서 API 요청 모니터링

**React DevTools 설치** (권장):
- [Chrome 확장](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi)
- 컴포넌트 트리 및 Props/State 검사

#### VS Code 디버깅

`.vscode/launch.json` 설정:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Backend",
      "program": "${workspaceFolder}/server/index.js",
      "cwd": "${workspaceFolder}/server"
    }
  ]
}
```

### 일반적인 개발 작업

#### API 엔드포인트 추가

**1. Backend 라우트 추가** (`server/index.js` 또는 별도 라우트 파일):

```javascript
// server/index.js
app.get('/api/new-endpoint', async (req, res) => {
  try {
    // 비즈니스 로직
    const data = await fetchDataFromSheets();
    res.json({ success: true, data });
  } catch (error) {
    console.error('Error:', error);
    res.status(500).json({ success: false, error: error.message });
  }
});
```

**2. Frontend에서 호출** (`src/utils/api.js` 또는 컴포넌트):

```javascript
import { API_BASE_URL } from '../api';

export const fetchNewData = async () => {
  const response = await fetch(`${API_BASE_URL}/api/new-endpoint`);
  if (!response.ok) {
    throw new Error('Failed to fetch data');
  }
  return response.json();
};
```

**3. 컴포넌트에서 사용**:

```javascript
import { fetchNewData } from '../utils/api';

const MyComponent = () => {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetchNewData()
      .then(result => setData(result.data))
      .catch(error => console.error(error));
  }, []);

  return <div>{/* UI */}</div>;
};
```

#### 새 컴포넌트 추가

```javascript
// src/components/NewComponent.js
import React, { useState, useEffect } from 'react';
import { Box, Typography, Button } from '@mui/material';
import { API_BASE_URL } from '../api';

const NewComponent = ({ prop1, prop2 }) => {
  const [state, setState] = useState(null);

  useEffect(() => {
    // 초기화 로직
  }, []);

  const handleAction = () => {
    // 이벤트 핸들러
  };

  return (
    <Box>
      <Typography variant="h5">새 컴포넌트</Typography>
      <Button onClick={handleAction}>액션</Button>
    </Box>
  );
};

export default NewComponent;
```

---

## 테스트 실행

### Frontend 테스트

```bash
# 모든 테스트 실행
npm test

# Watch 모드 (파일 변경 시 자동 실행)
npm test -- --watch

# 커버리지 리포트
npm test -- --coverage

# 특정 파일만 테스트
npm test -- api.test.js
```

### Backend 테스트

```bash
cd server

# 모든 테스트 실행
npm test

# Watch 모드
npm run test:watch

# 커버리지
npm run test:coverage

# 특정 테스트 파일
npm test -- cors.test.js
```

### Property-Based 테스트

Backend는 `fast-check`를 사용한 Property-Based Testing을 포함합니다:

```bash
cd server
npm test -- cors-properties.test.js
```

**Property-Based 테스트란?**
- 수많은 랜덤 입력으로 속성(property)을 검증
- 엣지 케이스 자동 발견
- 예: CORS 설정이 모든 유효한 오리진에 대해 작동하는지 확인

---

## 트러블슈팅

### 일반적인 문제 및 해결 방법


#### 문제 1: CORS 오류

**증상**:
```
Access to fetch at 'http://localhost:4000/api/stores' from origin 'http://localhost:3000' 
has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present
```

**원인**: Backend의 `ALLOWED_ORIGINS`에 Frontend URL이 없음

**해결 방법**:

1. **Backend 환경 변수 확인**:
```bash
# server/.env
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

2. **Backend 서버 재시작**:
```bash
cd server
npm run dev
```

3. **Backend 로그 확인**:
```
✅ [CORS] 설정 로드 완료
📋 [CORS] 허용된 오리진 목록:
  - http://localhost:3000
  - http://127.0.0.1:3000
```

4. **브라우저 캐시 삭제** (Ctrl+Shift+Delete)

#### 문제 2: 환경 변수가 적용되지 않음

**증상**: `.env` 파일을 수정했지만 여전히 기본값 사용

**원인**: 
- Frontend: 재빌드하지 않음
- Backend: 서버를 재시작하지 않음

**해결 방법**:

**Frontend**:
```bash
# 개발 서버 중지 (Ctrl+C)
# 재시작
npm start
```

**Backend**:
```bash
# 서버 중지 (Ctrl+C)
# 재시작
cd server
npm run dev
```

⚠️ **중요**: React 환경 변수는 빌드 시에 번들에 포함되므로, 변경 후 반드시 재시작이 필요합니다.

#### 문제 3: 포트가 이미 사용 중

**증상**:
```
Error: listen EADDRINUSE: address already in use :::3000
```

**원인**: 해당 포트를 사용하는 다른 프로세스가 실행 중

**해결 방법**:

**macOS/Linux**:
```bash
# 포트 사용 프로세스 확인
lsof -i :3000

# 프로세스 종료
kill -9 <PID>
```

**Windows**:
```bash
# 포트 사용 프로세스 확인
netstat -ano | findstr :3000

# 프로세스 종료
taskkill /PID <PID> /F
```

**또는 다른 포트 사용**:
```bash
# Frontend
PORT=3001 npm start

# Backend
PORT=4001 npm run dev
```

#### 문제 4: Google Sheets 연결 실패

**증상**:
```
❌ Google Sheets 인증 실패
Error: invalid_grant
```

**원인**:
- Service Account 권한 부족
- Private Key 형식 오류
- Sheet ID 오류

**해결 방법**:

1. **환경 변수 형식 확인**:
```bash
# server/.env
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----"
```

2. **Google Sheets 공유 확인**:
   - Google Sheets를 Service Account 이메일과 공유
   - 편집자 권한 부여

3. **Sheet ID 확인**:
   - URL에서 Sheet ID 복사: `https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit`

4. **팀 리더에게 인증 정보 요청**

#### 문제 5: npm install 실패

**증상**:
```
npm ERR! code ERESOLVE
npm ERR! ERESOLVE unable to resolve dependency tree
```

**원인**: 의존성 충돌

**해결 방법**:

```bash
# 1. node_modules 및 package-lock.json 삭제
rm -rf node_modules package-lock.json

# 2. npm 캐시 정리
npm cache clean --force

# 3. 재설치
npm install

# 또는 레거시 peer deps 사용
npm install --legacy-peer-deps
```

#### 문제 6: 빌드 실패 (ESLint 오류)

**증상**:
```
Failed to compile.
./src/components/MyComponent.js
  Line 10:5:  'variable' is assigned a value but never used  no-unused-vars
```

**원인**: ESLint 규칙 위반

**해결 방법**:

**1. 코드 수정** (권장):
```javascript
// 사용하지 않는 변수 제거
```

**2. 임시로 ESLint 비활성화**:
```bash
# .env
DISABLE_ESLINT_PLUGIN=true
```

**3. 특정 줄만 무시**:
```javascript
// eslint-disable-next-line no-unused-vars
const variable = 'value';
```

#### 문제 7: API 요청이 잘못된 URL로 전송됨

**증상**: Network 탭에서 `https://vipmobile-backend.cloudtype.app`로 요청이 가는데, 로컬 서버를 사용하고 싶음

**원인**: 
- 환경 변수가 설정되지 않음
- 하드코딩된 URL 사용
- `API_BASE_URL` import 누락

**해결 방법**:

1. **환경 변수 확인**:
```bash
# .env
REACT_APP_API_URL=http://localhost:4000
```

2. **컴포넌트에서 API_BASE_URL 사용 확인**:
```javascript
// ✅ 올바른 방법
import { API_BASE_URL } from '../api';
const response = await fetch(`${API_BASE_URL}/api/stores`);

// ❌ 잘못된 방법
const response = await fetch('https://vipmobile-backend.cloudtype.app/api/stores');
```

3. **개발 서버 재시작**

#### 문제 8: 지도가 표시되지 않음

**증상**: 지도 영역이 회색으로 표시되거나 타일이 로드되지 않음

**원인**:
- Leaflet CSS 누락
- 네트워크 문제
- 잘못된 좌표

**해결 방법**:

1. **Leaflet CSS 확인** (`public/index.html`):
```html
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
```

2. **브라우저 콘솔 확인**: 타일 로드 오류 메시지 확인

3. **좌표 확인**: 유효한 위도/경도 값인지 확인

#### 문제 9: 로그인 실패

**증상**: 로그인 버튼 클릭 시 오류 또는 무반응

**원인**:
- Google Sheets 연결 실패
- 잘못된 사용자 정보
- Backend 서버 다운

**해결 방법**:

1. **Backend 서버 실행 확인**:
```bash
curl http://localhost:4000/health
```

2. **Network 탭에서 API 요청 확인**

3. **Backend 로그 확인**: 인증 관련 오류 메시지

4. **테스트 계정 정보 확인**: 팀 리더에게 문의

### 디버깅 팁

#### Console 로그 활용

```javascript
// 개발 환경에서만 로그 출력
if (process.env.NODE_ENV === 'development') {
  console.log('Debug info:', data);
}

// 또는 환경 변수 사용
if (process.env.REACT_APP_LOGGING_ENABLED === 'true') {
  console.log('Debug info:', data);
}
```

#### Network 탭 활용

1. F12 → Network 탭
2. API 요청 클릭
3. **Headers**: 요청/응답 헤더 확인
4. **Preview**: 응답 데이터 미리보기
5. **Response**: 원본 응답 확인
6. **Timing**: 요청 시간 분석

#### React DevTools 활용

1. React DevTools 설치
2. Components 탭에서 컴포넌트 트리 확인
3. Props 및 State 검사
4. Profiler로 성능 분석

---

## 코딩 가이드라인

### JavaScript/React 스타일

#### 컴포넌트 구조

```javascript
// 1. Imports
import React, { useState, useEffect } from 'react';
import { Box, Typography } from '@mui/material';
import { API_BASE_URL } from '../api';

// 2. 컴포넌트 정의
const MyComponent = ({ prop1, prop2 }) => {
  // 3. State
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(false);

  // 4. Effects
  useEffect(() => {
    fetchData();
  }, []);

  // 5. 함수
  const fetchData = async () => {
    setLoading(true);
    try {
      const response = await fetch(`${API_BASE_URL}/api/data`);
      const result = await response.json();
      setData(result);
    } catch (error) {
      console.error('Error:', error);
    } finally {
      setLoading(false);
    }
  };

  // 6. Render
  return (
    <Box>
      <Typography>{data?.title}</Typography>
    </Box>
  );
};

// 7. Export
export default MyComponent;
```

#### 명명 규칙

- **컴포넌트**: PascalCase (`MyComponent`)
- **함수/변수**: camelCase (`fetchData`, `userData`)
- **상수**: UPPER_SNAKE_CASE (`API_BASE_URL`, `MAX_RETRIES`)
- **파일명**: 컴포넌트는 PascalCase, 유틸리티는 camelCase

#### API 호출 패턴

```javascript
// ✅ 권장: async/await with try-catch
const fetchData = async () => {
  try {
    const response = await fetch(`${API_BASE_URL}/api/data`);
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('Failed to fetch data:', error);
    throw error;
  }
};

// ❌ 비권장: Promise chaining without error handling
fetch(`${API_BASE_URL}/api/data`)
  .then(res => res.json())
  .then(data => console.log(data));
```

