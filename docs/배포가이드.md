# VIP Map Application 배포 가이드

## 📋 목차

1. [개요](#개요)
2. [환경 변수 설정](#환경-변수-설정)
3. [Frontend 배포 (Vercel)](#frontend-배포-vercel)
4. [Backend 배포 (Cloudtype)](#backend-배포-cloudtype)
5. [Android 앱 빌드](#android-앱-빌드)
6. [배포 검증](#배포-검증)
7. [문제 해결](#문제-해결)

---

## 개요

VIP Map Application은 다음 세 가지 컴포넌트로 구성됩니다:

- **Frontend**: React 기반 웹 애플리케이션 (Vercel 배포)
- **Backend**: Node.js/Express 서버 (Cloudtype 배포)
- **Android App**: Kotlin 기반 모바일 앱 (APK 빌드)

이 가이드는 각 컴포넌트의 배포 방법과 환경 변수 설정 방법을 설명합니다.

### 주요 URL

- **Frontend (Production)**: `https://vipmobile.vercel.app`
- **Backend (Production)**: `https://vipmobile-backend.cloudtype.app`
- **Backend (실제 서버)**: `https://port-0-vipmobile-mh7msgrz3167a0bf.sel3.cloudtype.app`

---

## 환경 변수 설정

### 환경 변수 개요

각 컴포넌트는 환경에 따라 다른 설정을 사용합니다:

| 환경 변수 | Frontend | Backend | Android |
|----------|----------|---------|---------|
| API URL | ✅ | ❌ | ✅ |
| CORS Origins | ❌ | ✅ | ❌ |
| Google Sheets | ❌ | ✅ | ❌ |
| Discord Bot | ❌ | ✅ | ❌ |

### 환경별 설정

#### Development (개발)
- Frontend: `http://localhost:3000`
- Backend: `http://localhost:4000`
- Android: `http://10.0.2.2:4000` (에뮬레이터)

#### Production (프로덕션)
- Frontend: `https://vipmobile.vercel.app`
- Backend: `https://vipmobile-backend.cloudtype.app`
- Android: `https://vipmobile-backend.cloudtype.app`

---

## Frontend 배포 (Vercel)

### 1. 사전 준비

#### 필수 요구사항
- Vercel 계정
- GitHub 저장소 연결
- Node.js 22.x

#### 환경 변수 목록

Frontend는 다음 환경 변수를 사용합니다:

| 변수명 | 설명 | 필수 | 기본값 |
|--------|------|------|--------|
| `REACT_APP_API_URL` | 백엔드 API URL | ✅ | `https://vipmobile-backend.cloudtype.app` |
| `REACT_APP_LOGGING_ENABLED` | 로깅 활성화 | ❌ | `true` |
| `REACT_APP_PUSH_NOTIFICATIONS_ENABLED` | 푸시 알림 활성화 | ❌ | `true` |
| `REACT_APP_DISABLE_PRICE_CACHE` | 가격 캐시 비활성화 | ❌ | `true` |
| `GENERATE_SOURCEMAP` | 소스맵 생성 | ❌ | `false` |
| `CI` | CI 환경 | ❌ | `false` |

### 2. Vercel 대시보드 설정

#### Step 1: 프로젝트 생성/선택

1. [Vercel 대시보드](https://vercel.com/dashboard) 접속
2. 기존 프로젝트 선택 또는 **New Project** 클릭
3. GitHub 저장소 `sonigay/vipmobile` 선택
4. **Import** 클릭

#### Step 2: 빌드 설정

**Framework Preset**: `Create React App`

**Build Command**:
```bash
npm run build:ultra-fast
```

**Output Directory**:
```
build
```

**Install Command**:
```bash
npm ci
```

#### Step 3: 환경 변수 설정

**Settings** → **Environment Variables**로 이동하여 다음 변수들을 추가합니다:

**프로덕션 환경 변수**:
```bash
# API 설정
REACT_APP_API_URL=https://vipmobile-backend.cloudtype.app

# 기능 플래그
REACT_APP_LOGGING_ENABLED=true
REACT_APP_PUSH_NOTIFICATIONS_ENABLED=true
REACT_APP_DISABLE_PRICE_CACHE=true

# 빌드 최적화
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false
CI=false
NODE_ENV=production

# 개발 도구 비활성화
SKIP_PREFLIGHT_CHECK=true
FAST_REFRESH=false
TSC_COMPILE_ON_ERROR=false
ESLINT_NO_DEV_ERRORS=true
DISABLE_ESLINT_PLUGIN=true
```

⚠️ **중요**: 각 환경 변수는 **Production**, **Preview**, **Development** 환경을 선택할 수 있습니다. 프로덕션 배포의 경우 **Production**을 선택하세요.

### 3. vercel.json 설정 파일

프로젝트 루트에 `vercel.json` 파일이 있어야 합니다:

```json
{
  "buildCommand": "npm run build:ultra-fast",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "env": {
    "REACT_APP_API_URL": "https://vipmobile-backend.cloudtype.app",
    "REACT_APP_LOGGING_ENABLED": "true",
    "REACT_APP_PUSH_NOTIFICATIONS_ENABLED": "true",
    "REACT_APP_DISABLE_PRICE_CACHE": "true",
    "GENERATE_SOURCEMAP": "false",
    "INLINE_RUNTIME_CHUNK": "false",
    "CI": "false",
    "NODE_ENV": "production",
    "SKIP_PREFLIGHT_CHECK": "true",
    "FAST_REFRESH": "false",
    "TSC_COMPILE_ON_ERROR": "false",
    "ESLINT_NO_DEV_ERRORS": "true",
    "DISABLE_ESLINT_PLUGIN": "true"
  }
}
```

### 4. 배포 실행

#### 자동 배포 (권장)

1. `main` 브랜치에 코드 푸시
2. Vercel이 자동으로 빌드 및 배포 시작
3. 배포 진행 상황은 Vercel 대시보드에서 확인

#### 수동 배포

```bash
# Vercel CLI 설치 (최초 1회)
npm install -g vercel

# 로그인
vercel login

# 배포
vercel --prod
```

### 5. 배포 확인

배포가 완료되면:

1. Vercel 대시보드에서 **Visit** 버튼 클릭
2. 브라우저 개발자 도구 (F12) 열기
3. **Console** 탭에서 오류 확인
4. **Network** 탭에서 API 요청 확인

**성공 지표**:
- ✅ 페이지가 정상적으로 로드됨
- ✅ Console에 CORS 오류 없음
- ✅ API 요청이 200 상태 코드 반환
- ✅ 지도와 데이터가 정상 표시됨

---

## Backend 배포 (Cloudtype)

### 1. 사전 준비

#### 필수 요구사항
- Cloudtype 계정
- GitHub 저장소 연결
- Google Service Account 인증 정보
- Discord Bot 토큰 (선택사항)

#### 환경 변수 목록

Backend는 다음 환경 변수를 사용합니다:

| 변수명 | 설명 | 필수 | 예시 |
|--------|------|------|------|
| `PORT` | 서버 포트 | ❌ | `4000` |
| `NODE_ENV` | 실행 환경 | ✅ | `production` |
| `ALLOWED_ORIGINS` | CORS 허용 오리진 | ✅ | 아래 참조 |
| `SHEET_ID` | Google Sheets ID | ✅ | `1abc...xyz` |
| `GOOGLE_SERVICE_ACCOUNT_EMAIL` | Google 서비스 계정 이메일 | ✅ | `xxx@xxx.iam.gserviceaccount.com` |
| `GOOGLE_PRIVATE_KEY` | Google 서비스 계정 Private Key | ✅ | `-----BEGIN PRIVATE KEY-----\n...` |
| `DISCORD_BOT_TOKEN` | Discord Bot 토큰 | ❌ | `MTxxx...` |
| `DISCORD_CHANNEL_ID` | Discord 채널 ID | ❌ | `123456789` |
| `DISCORD_LOGGING_ENABLED` | Discord 로깅 활성화 | ❌ | `true` |
| `KAKAO_API_KEY` | Kakao Maps API 키 | ❌ | `xxx` |
| `VAPID_PUBLIC_KEY` | Web Push 공개 키 | ❌ | `xxx` |
| `VAPID_PRIVATE_KEY` | Web Push 비밀 키 | ❌ | `xxx` |

### 2. Cloudtype 대시보드 설정

#### Step 1: 프로젝트 생성/선택

1. [Cloudtype 대시보드](https://cloudtype.io/) 접속
2. 로그인 후 프로젝트 선택 또는 **새 프로젝트** 생성
3. GitHub 저장소 `sonigay/vipmobile` 연결

#### Step 2: 빌드 설정

**빌드 경로**: `/server`

**빌드 명령어**:
```bash
npm ci --production
```

**시작 명령어**:
```bash
node index.js
```

**포트**: `4000`

#### Step 3: 환경 변수 설정 (중요!)

**Settings** → **Environment Variables**로 이동하여 다음 변수들을 추가합니다:

##### 필수 환경 변수

**1. ALLOWED_ORIGINS** (CORS 설정)

이 변수는 **가장 중요**합니다. CORS 오류를 방지하기 위해 모든 프론트엔드 URL을 포함해야 합니다.

```bash
ALLOWED_ORIGINS=https://vipmobile.vercel.app,https://port-0-vipmobile-mh7msgrz3167a0bf.sel3.cloudtype.app,https://vipmobile-backend.cloudtype.app,http://localhost:3000
```

⚠️ **주의사항**:
- 쉼표(`,`) 사이에 **공백 없음**
- 각 URL은 프로토콜(`https://`) 포함
- URL 끝에 슬래시(`/`) 없음
- Vercel 배포 URL 포함 필수
- 실제 Cloudtype 서버 URL 포함 필수

**2. NODE_ENV**
```bash
NODE_ENV=production
```

**3. PORT**
```bash
PORT=4000
```

**4. Google Sheets 설정**

```bash
SHEET_ID=your_google_sheet_id_here
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----
```

⚠️ **GOOGLE_PRIVATE_KEY 주의사항**:
- Private Key는 `\n`으로 줄바꿈을 표현해야 합니다
- `-----BEGIN PRIVATE KEY-----`와 `-----END PRIVATE KEY-----` 포함
- 전체를 큰따옴표로 감싸지 않음

##### 선택적 환경 변수

**Discord 로깅** (선택사항):
```bash
DISCORD_BOT_TOKEN=your_discord_bot_token
DISCORD_CHANNEL_ID=your_discord_channel_id
DISCORD_LOGGING_ENABLED=true
```

**Kakao Maps** (선택사항):
```bash
KAKAO_API_KEY=your_kakao_api_key
```

**Web Push** (선택사항):
```bash
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
```

### 3. cloudtype.yml 설정 파일

프로젝트의 `cloudtype.yml` 파일:

```yaml
# Cloudtype 설정 파일
build:
  command: npm ci --production
  path: /server

start:
  command: node index.js

resources:
  requests:
    memory: "32Mi"
    cpu: "10m"
  limits:
    memory: "256Mi"
    cpu: "200m"

env:
  NODE_ENV: production
  PORT: 4000

deploy:
  auto: true
  branch: main
  path: /server
  webhook: true
  github:
    repository: sonigay/vipmobile
    branch: main
```

### 4. 배포 실행

#### 자동 배포 (권장)

1. `main` 브랜치에 코드 푸시
2. Cloudtype이 자동으로 빌드 및 배포 시작
3. 배포 진행 상황은 Cloudtype 대시보드에서 확인

#### 수동 배포

Cloudtype 대시보드에서:
1. **재배포** 또는 **Redeploy** 버튼 클릭
2. 빌드 로그 확인
3. 배포 완료 대기

### 5. 배포 확인

#### 로그 확인

Cloudtype 대시보드 → **로그** 또는 **Console**에서 다음 메시지를 확인:

**성공적인 시작 로그**:
```
✅ [CORS] 설정 로드 완료
📋 [CORS] 허용된 오리진 목록:
  - https://vipmobile.vercel.app
  - https://port-0-vipmobile-mh7msgrz3167a0bf.sel3.cloudtype.app
  - https://vipmobile-backend.cloudtype.app
  - http://localhost:3000
🔧 [CORS] 설정 출처: environment
🚀 Server is running on port 4000
```

#### API 테스트

터미널에서 다음 명령어로 서버 상태 확인:

```bash
# 헬스 체크
curl https://vipmobile-backend.cloudtype.app/health

# CORS 테스트
curl -H "Origin: https://vipmobile.vercel.app" \
     -H "Access-Control-Request-Method: GET" \
     -H "Access-Control-Request-Headers: Content-Type" \
     -X OPTIONS \
     --verbose \
     https://vipmobile-backend.cloudtype.app/api/stores
```

**성공 응답**:
```
< HTTP/1.1 204 No Content
< Access-Control-Allow-Origin: https://vipmobile.vercel.app
< Access-Control-Allow-Methods: GET,HEAD,PUT,PATCH,POST,DELETE
< Access-Control-Allow-Credentials: true
```

---

## Android 앱 빌드

### 1. 사전 준비

#### 필수 요구사항
- Android Studio Arctic Fox 이상
- JDK 8 이상
- Android SDK (API Level 21-33)
- Keystore 파일 (Release 빌드용)

#### 프로젝트 구조
```
android-app/
├── app/
│   ├── build.gradle          # 앱 레벨 빌드 설정
│   ├── src/
│   │   └── main/
│   │       ├── java/com/vipplus/manager/
│   │       │   ├── MainActivity.kt
│   │       │   ├── ManagerService.kt
│   │       │   └── ApiClient.kt
│   │       └── AndroidManifest.xml
│   └── release.keystore      # Release 서명용 키스토어
└── build.gradle              # 프로젝트 레벨 빌드 설정
```

### 2. API URL 설정

Android 앱은 `build.gradle`에서 빌드 타입별로 다른 API URL을 사용합니다.

#### build.gradle 설정

`android-app/app/build.gradle` 파일:

```gradle
android {
    compileSdk 33

    defaultConfig {
        applicationId "com.vipplus.manager"
        minSdk 21
        targetSdk 33
        versionCode 9
        versionName "2.1.6"
        
        // 기본 API URL (모든 빌드 타입의 기본값)
        buildConfigField "String", "API_BASE_URL", "\"https://vipmobile-backend.cloudtype.app\""
    }
    
    buildFeatures {
        buildConfig true
    }

    buildTypes {
        debug {
            // 개발 환경: Android 에뮬레이터에서 호스트 머신의 localhost 접근
            buildConfigField "String", "API_BASE_URL", "\"http://10.0.2.2:4000\""
        }
        
        release {
            // 프로덕션 환경
            buildConfigField "String", "API_BASE_URL", "\"https://vipmobile-backend.cloudtype.app\""
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}
```

#### MainActivity.kt에서 사용

`MainActivity.kt`에서 BuildConfig를 통해 API URL을 가져옵니다:

```kotlin
// BuildConfig에서 API URL 가져오기 (빌드 타입별로 다른 URL 사용)
val defaultUrl = try {
    BuildConfig.API_BASE_URL
} catch (e: Exception) {
    android.util.Log.e("MainActivity", "Failed to load API_BASE_URL from BuildConfig", e)
    "https://vipmobile-backend.cloudtype.app" // 폴백 기본값
}
```

### 3. 빌드 타입별 설정

#### Debug 빌드 (개발용)

**API URL**: `http://10.0.2.2:4000`
- Android 에뮬레이터에서 호스트 머신의 `localhost:4000` 접근
- 실제 디바이스에서는 작동하지 않음 (로컬 네트워크 IP 사용 필요)

**빌드 명령어**:
```bash
cd android-app
./gradlew assembleDebug
```

**출력 파일**:
```
android-app/app/build/outputs/apk/debug/app-debug.apk
```

#### Release 빌드 (프로덕션용)

**API URL**: `https://vipmobile-backend.cloudtype.app`

**빌드 명령어**:
```bash
cd android-app
./gradlew assembleRelease
```

**출력 파일**:
```
android-app/app/build/outputs/apk/release/app-release.apk
```

### 4. Keystore 설정 (Release 빌드)

Release 빌드는 서명이 필요합니다.

#### 환경 변수 방식 (권장)

```bash
# 환경 변수 설정
export KEYSTORE_FILE=/path/to/release.keystore
export KEYSTORE_PASSWORD=your_keystore_password
export KEY_ALIAS=vipmanager
export KEY_PASSWORD=your_key_password

# 빌드
./gradlew assembleRelease
```

#### 기본값 방식

환경 변수가 설정되지 않으면 다음 기본값 사용:
- Keystore 파일: `release.keystore` (app 폴더 내)
- Keystore 비밀번호: `vipmanager2024`
- Key Alias: `vipmanager`
- Key 비밀번호: `vipmanager2024`

### 5. Android Studio에서 빌드

#### Step 1: 프로젝트 열기

1. Android Studio 실행
2. **Open an Existing Project** 선택
3. `android-app` 폴더 선택

#### Step 2: 빌드 타입 선택

1. **Build** → **Select Build Variant**
2. **debug** 또는 **release** 선택

#### Step 3: APK 빌드

1. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. 빌드 완료 후 **locate** 링크 클릭하여 APK 파일 위치 확인

### 6. API URL 변경 방법

프로덕션 API URL을 변경해야 하는 경우:

#### 방법 1: build.gradle 수정 (권장)

`android-app/app/build.gradle` 파일에서:

```gradle
buildTypes {
    release {
        buildConfigField "String", "API_BASE_URL", "\"https://new-api-url.com\""
    }
}
```

#### 방법 2: 런타임 설정

앱 실행 후 MainActivity에서 서버 URL을 수동으로 변경할 수 있습니다:
1. 앱 실행
2. 서버 URL 입력 필드에 새 URL 입력
3. **저장** 버튼 클릭

이 방법은 SharedPreferences에 저장되어 앱 재시작 후에도 유지됩니다.

### 7. 빌드 검증

#### Debug 빌드 테스트

1. 에뮬레이터 또는 실제 디바이스에 설치
2. 로컬 백엔드 서버 실행 (`cd server && npm start`)
3. 앱에서 **연결 테스트** 버튼 클릭
4. "✅ 서버 연결 성공!" 메시지 확인

#### Release 빌드 테스트

1. 실제 디바이스에 설치
2. 앱에서 **연결 테스트** 버튼 클릭
3. 프로덕션 서버와 연결 확인
4. SMS 수신 및 전송 기능 테스트

### 8. 버전 관리

앱 버전은 `build.gradle`에서 관리:

```gradle
defaultConfig {
    versionCode 9        // 정수, 각 릴리스마다 증가
    versionName "2.1.6"  // 사용자에게 표시되는 버전
}
```

**버전 업데이트 규칙**:
- `versionCode`: 매 릴리스마다 1씩 증가 (필수)
- `versionName`: Semantic Versioning (MAJOR.MINOR.PATCH)

---

## 배포 검증

### 1. Frontend 검증

#### 브라우저 테스트

1. **프로덕션 URL 접속**
   ```
   https://vipmobile.vercel.app
   ```

2. **개발자 도구 확인** (F12)
   - **Console 탭**: CORS 오류 없는지 확인
   - **Network 탭**: API 요청 상태 확인
   - **Application 탭**: LocalStorage 데이터 확인

3. **기능 테스트**
   - [ ] 로그인 성공
   - [ ] 지도 로드 및 마커 표시
   - [ ] 매장 목록 조회
   - [ ] 재고 데이터 로드
   - [ ] 모드 전환 (권한에 따라)

#### 성공 지표

**Console 로그 예시**:
```
ℹ️ REACT_APP_API_URL not set. Using default: https://vipmobile-backend.cloudtype.app
✅ API 연결 성공
✅ 사용자 인증 완료
```

**Network 요청 예시**:
```
GET https://vipmobile-backend.cloudtype.app/api/stores
Status: 200 OK
Response Headers:
  Access-Control-Allow-Origin: https://vipmobile.vercel.app
  Content-Type: application/json
```

### 2. Backend 검증

#### 헬스 체크

```bash
# 서버 상태 확인
curl https://vipmobile-backend.cloudtype.app/health

# 예상 응답
{
  "status": "ok",
  "timestamp": "2024-01-15T10:30:00.000Z"
}
```

#### CORS 검증

```bash
# Vercel에서의 요청 시뮬레이션
curl -H "Origin: https://vipmobile.vercel.app" \
     -H "Access-Control-Request-Method: GET" \
     -X OPTIONS \
     --verbose \
     https://vipmobile-backend.cloudtype.app/api/stores

# 성공 응답 확인
< Access-Control-Allow-Origin: https://vipmobile.vercel.app
< Access-Control-Allow-Credentials: true
```

#### 로그 확인

Cloudtype 대시보드에서 로그 확인:

**정상 시작 로그**:
```
✅ [CORS] 설정 로드 완료
📋 [CORS] 허용된 오리진 목록:
  - https://vipmobile.vercel.app
  - https://port-0-vipmobile-mh7msgrz3167a0bf.sel3.cloudtype.app
  - https://vipmobile-backend.cloudtype.app
  - http://localhost:3000
🔧 [CORS] 설정 출처: environment
🚀 Server is running on port 4000
✅ Google Sheets 연결 성공
```

**API 요청 로그**:
```
[2024-01-15 10:30:00] GET /api/stores - 200 (125ms)
[2024-01-15 10:30:01] GET /api/inventory - 200 (89ms)
```

### 3. Android 앱 검증

#### 설치 및 실행

1. **APK 설치**
   ```bash
   adb install android-app/app/build/outputs/apk/release/app-release.apk
   ```

2. **앱 실행**
   - 앱 아이콘 클릭
   - 권한 요청 승인 (SMS, 전화 상태)

3. **서버 연결 테스트**
   - **연결 테스트** 버튼 클릭
   - "✅ 서버 연결 성공!" 메시지 확인

#### 로그 확인

```bash
# Android 로그 확인
adb logcat | grep MainActivity

# 예상 로그
D/MainActivity: Build Type: release
D/MainActivity: API Base URL: https://vipmobile-backend.cloudtype.app
I/MainActivity: ✅ 서버 연결 성공
```

### 4. 통합 테스트

#### End-to-End 시나리오

1. **웹에서 데이터 입력**
   - Vercel 배포 사이트 접속
   - 재고 데이터 추가/수정

2. **백엔드 처리 확인**
   - Cloudtype 로그에서 API 요청 확인
   - Google Sheets에 데이터 반영 확인

3. **Android 앱에서 확인**
   - SMS 수신 테스트
   - 서버로 데이터 전송 확인

#### 성능 테스트

```bash
# API 응답 시간 측정
time curl https://vipmobile-backend.cloudtype.app/api/stores

# 예상 결과: < 500ms
```

---

## 문제 해결

### Frontend 문제

#### 문제 1: CORS 오류 발생

**증상**:
```
Access to fetch at 'https://vipmobile-backend.cloudtype.app/api/stores' 
from origin 'https://vipmobile.vercel.app' has been blocked by CORS policy
```

**원인**:
- Backend의 `ALLOWED_ORIGINS`에 Frontend URL이 없음
- URL 형식 불일치 (http vs https, 슬래시 유무)

**해결 방법**:
1. Cloudtype 환경 변수 확인
2. `ALLOWED_ORIGINS`에 정확한 URL 추가
3. 서버 재시작
4. 브라우저 캐시 삭제

#### 문제 2: 환경 변수가 적용되지 않음

**증상**:
- Console에 기본 URL이 표시됨
- API 요청이 잘못된 서버로 전송됨

**원인**:
- Vercel 환경 변수 미설정
- 빌드 후 환경 변수 변경 (재빌드 필요)

**해결 방법**:
1. Vercel 대시보드에서 환경 변수 확인
2. 변수명이 `REACT_APP_` 접두사로 시작하는지 확인
3. **Redeploy** 버튼 클릭하여 재배포

#### 문제 3: 빌드 실패

**증상**:
```
Error: Build failed with exit code 1
```

**원인**:
- ESLint 오류
- TypeScript 컴파일 오류
- 메모리 부족

**해결 방법**:
1. `vercel.json`에 다음 설정 확인:
   ```json
   {
     "env": {
       "CI": "false",
       "DISABLE_ESLINT_PLUGIN": "true"
     }
   }
   ```
2. 로컬에서 빌드 테스트: `npm run build:ultra-fast`
3. 오류 메시지 확인 후 수정

### Backend 문제

#### 문제 1: 서버 시작 실패

**증상**:
- Cloudtype 로그에 오류 메시지
- 서버 상태가 "Failed"

**원인**:
- 환경 변수 누락 (특히 Google Sheets 관련)
- `GOOGLE_PRIVATE_KEY` 형식 오류
- 포트 충돌

**해결 방법**:
1. 필수 환경 변수 확인:
   - `NODE_ENV`
   - `PORT`
   - `SHEET_ID`
   - `GOOGLE_SERVICE_ACCOUNT_EMAIL`
   - `GOOGLE_PRIVATE_KEY`

2. `GOOGLE_PRIVATE_KEY` 형식 확인:
   ```
   -----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----
   ```

3. 로그에서 구체적인 오류 메시지 확인

#### 문제 2: Google Sheets 연결 실패

**증상**:
```
❌ Google Sheets 인증 실패
Error: invalid_grant
```

**원인**:
- Service Account 권한 부족
- Private Key 형식 오류
- Sheet ID 오류

**해결 방법**:
1. Google Cloud Console에서 Service Account 확인
2. Sheet에 Service Account 이메일 공유 (편집자 권한)
3. Private Key 재생성 및 환경 변수 업데이트

#### 문제 3: 메모리 부족

**증상**:
```
JavaScript heap out of memory
```

**원인**:
- Cloudtype 리소스 제한
- 메모리 누수

**해결 방법**:
1. `cloudtype.yml`에서 메모리 제한 증가:
   ```yaml
   resources:
     limits:
       memory: "512Mi"
   ```
2. 캐시 TTL 조정하여 메모리 사용량 감소

### Android 앱 문제

#### 문제 1: 서버 연결 실패

**증상**:
- "❌ 서버 연결 실패" 메시지
- 네트워크 타임아웃

**원인**:
- 잘못된 API URL
- 네트워크 권한 부족
- 서버 다운

**해결 방법**:
1. `AndroidManifest.xml`에서 인터넷 권한 확인:
   ```xml
   <uses-permission android:name="android.permission.INTERNET" />
   ```

2. 앱에서 서버 URL 확인 및 수정

3. 브라우저에서 API URL 접속 테스트

#### 문제 2: BuildConfig 오류

**증상**:
```
Unresolved reference: BuildConfig
```

**원인**:
- `buildFeatures { buildConfig true }` 누락
- Gradle 동기화 실패

**해결 방법**:
1. `build.gradle`에 다음 추가:
   ```gradle
   android {
       buildFeatures {
           buildConfig true
       }
   }
   ```

2. **File** → **Sync Project with Gradle Files**

3. **Build** → **Clean Project** 후 재빌드

#### 문제 3: 서명 오류

**증상**:
```
Execution failed for task ':app:validateSigningRelease'
```

**원인**:
- Keystore 파일 없음
- 잘못된 비밀번호

**해결 방법**:
1. Keystore 파일 경로 확인
2. 환경 변수 또는 `build.gradle`에서 비밀번호 확인
3. 필요시 새 Keystore 생성:
   ```bash
   keytool -genkey -v -keystore release.keystore \
     -alias vipmanager -keyalg RSA -keysize 2048 -validity 10000
   ```

---

## 배포 체크리스트

### Frontend 배포 전

- [ ] 로컬에서 빌드 테스트 (`npm run build:ultra-fast`)
- [ ] 환경 변수 확인 (`REACT_APP_API_URL`)
- [ ] `vercel.json` 설정 확인
- [ ] Git 커밋 및 푸시

### Frontend 배포 후

- [ ] Vercel 빌드 로그 확인
- [ ] 배포된 사이트 접속 테스트
- [ ] 브라우저 Console 오류 확인
- [ ] API 요청 정상 작동 확인
- [ ] 주요 기능 테스트 (로그인, 지도, 재고)

### Backend 배포 전

- [ ] 로컬에서 서버 테스트 (`npm start`)
- [ ] 환경 변수 준비 (특히 `ALLOWED_ORIGINS`)
- [ ] Google Sheets 권한 확인
- [ ] `cloudtype.yml` 설정 확인

### Backend 배포 후

- [ ] Cloudtype 빌드 로그 확인
- [ ] 서버 시작 로그 확인 (CORS 설정 포함)
- [ ] 헬스 체크 API 테스트
- [ ] CORS 테스트 (curl 명령어)
- [ ] Google Sheets 연결 확인

### Android 앱 빌드 전

- [ ] `build.gradle`에서 API URL 확인
- [ ] 버전 코드/이름 업데이트
- [ ] Keystore 파일 및 비밀번호 준비 (Release)
- [ ] 로컬에서 빌드 테스트

### Android 앱 빌드 후

- [ ] APK 파일 생성 확인
- [ ] 실제 디바이스에 설치 테스트
- [ ] 서버 연결 테스트
- [ ] SMS 수신/전송 테스트
- [ ] 권한 요청 정상 작동 확인

---

## 추가 리소스

### 관련 문서

- [API URL 중앙화 설계 문서](.kiro/specs/api-url-centralization/design.md)
- [Cloudtype 설정 가이드](.kiro/specs/api-url-centralization/CLOUDTYPE_SETUP_GUIDE.md)
- [CORS 테스트 가이드](server/__tests__/cors.test.js)

### 외부 링크

- [Vercel 문서](https://vercel.com/docs)
- [Cloudtype 문서](https://docs.cloudtype.io/)
- [Android Build Configuration](https://developer.android.com/studio/build)
- [React Environment Variables](https://create-react-app.dev/docs/adding-custom-environment-variables/)

### 지원

문제가 해결되지 않으면 다음 정보를 포함하여 문의하세요:

1. **Frontend 문제**:
   - Vercel 빌드 로그
   - 브라우저 Console 오류
   - Network 탭 스크린샷

2. **Backend 문제**:
   - Cloudtype 서버 로그 (최근 100줄)
   - 환경 변수 설정 (민감 정보 제외)
   - curl 테스트 결과

3. **Android 문제**:
   - Gradle 빌드 로그
   - Android Logcat 출력
   - `build.gradle` 설정

---

**작성일**: 2024-01-15  
**버전**: 1.0  
**관련 작업**: API URL 중앙화 (작업 12.1)  
**관련 요구사항**: Requirements 4.1, 4.2, 4.3, 6.4
