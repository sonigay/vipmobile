import React from 'react';
import PlaceholderModeScreen from './PlaceholderModeScreen';

const AttendanceMode = (props) => {
  return <PlaceholderModeScreen modeKey="attendance" {...props} />;
};

export default AttendanceMode;

