import React from 'react';
import PlaceholderModeScreen from './PlaceholderModeScreen';

const DirectStoreManagementMode = (props) => {
  return <PlaceholderModeScreen modeKey="directStoreManagement" {...props} />;
};

export default DirectStoreManagementMode;

