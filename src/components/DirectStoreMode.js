import React from 'react';
import PlaceholderModeScreen from './PlaceholderModeScreen';

const DirectStoreMode = (props) => {
  return <PlaceholderModeScreen modeKey="directStore" {...props} />;
};

export default DirectStoreMode;

