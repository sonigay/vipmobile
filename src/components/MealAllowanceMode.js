import React from 'react';
import PlaceholderModeScreen from './PlaceholderModeScreen';

const MealAllowanceMode = (props) => {
  return <PlaceholderModeScreen modeKey="mealAllowance" {...props} />;
};

export default MealAllowanceMode;

