import React from 'react';
import PlaceholderModeScreen from './PlaceholderModeScreen';

const QuickServiceManagementMode = (props) => {
  return <PlaceholderModeScreen modeKey="quickServiceManagement" {...props} />;
};

export default QuickServiceManagementMode;
