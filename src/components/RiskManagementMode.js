import React from 'react';
import PlaceholderModeScreen from './PlaceholderModeScreen';

const RiskManagementMode = (props) => {
  return <PlaceholderModeScreen modeKey="riskManagement" {...props} />;
};

export default RiskManagementMode;

