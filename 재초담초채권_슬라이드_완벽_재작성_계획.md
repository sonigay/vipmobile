# 재초담초채권 슬라이드 완벽 재작성 계획 (개선판)

## 🎯 목표
재초담초채권 슬라이드만 수정하고, **다른 모든 슬라이드에 영향을 주지 않도록** 보장

## ⚠️ 이전 플랜의 문제점 분석

### 문제점
1. **SlideCaptureConfig.js**: 모든 슬라이드 타입(main, toc, ending, default 등)에 `imageQuality` 설정 추가 → **다른 슬라이드에 불필요한 변경**
2. **UnifiedCaptureEngine.js**: 모든 `captureElement` 호출에 `imageQuality: config?.imageQuality` 전달 → **undefined가 전달되어 기존 로직 충돌**
3. **screenCapture.js**: 재초담초채권 감지 로직 제거 및 설정 기반으로 변경 → **다른 슬라이드의 기존 로직에 영향**
4. **MeetingCaptureManager.js**: 재초담초채권 특별 처리 완전 제거 → **업로드 안정성 저하 가능성**

### 근본 원인
- 재초담초채권 슬라이드만이 1.16MB로 상대적으로 크다는 점을 간과
- "모든 슬라이드를 동일하게 처리"하려는 접근이 오히려 기존 로직을 깨뜨림
- 다른 슬라이드의 검증된 로직을 변경하지 말아야 함

---

## ✅ 개선된 접근 방식

### 핵심 원칙
1. **재초담초채권 슬라이드만 식별하여 처리**
2. **다른 슬라이드는 기존 로직 완전히 유지 (변경 없음)**
3. **기존 검증된 로직은 최대한 보존**

---

## 📋 단계별 실행 계획

### 1단계: SlideCaptureConfig.js - 재초담초채권만 설정 추가

**파일**: `src/components/meeting/SlideCaptureConfig.js`

**변경 사항**:
- ✅ `rechotanchoBond` 설정에만 `imageQuality: 0.85` 추가
- ❌ 다른 슬라이드 설정은 **절대 변경하지 않음**

```javascript
// 재초담초채권
rechotanchoBond: {
  needsScrollRemoval: true,
  needsWidthAdjustment: true,
  needsHeightMeasurement: true,
  needsBoxResize: false,
  needsRightWhitespaceRemoval: true,
  needsDataLoadingWait: false,
  needsTableExpansion: false,
  captureMethod: 'direct',
  preserveHeader: false,
  needsHeaderComposition: false,
  needsHeaderSizeAdjustment: false,
  needsTableVerification: false,
  needsManagerTableInclusion: false,
  boxResizeIterations: 1,
  retryConfig: { maxRetries: 3, delay: 500 },
  imageQuality: 0.85,  // ✅ 재초담초채권만 추가 (다른 슬라이드는 추가하지 않음)
},
```

**검증 포인트**:
- ✅ `main`, `toc`, `ending`, `default` 등 다른 설정에는 `imageQuality`가 없음을 확인
- ✅ 다른 슬라이드 설정은 한 글자도 변경하지 않음

---

### 2단계: UnifiedCaptureEngine.js - 재초담초채권일 때만 imageQuality 전달

**파일**: `src/components/meeting/UnifiedCaptureEngine.js`

**변경 원칙**:
- ✅ 재초담초채권 슬라이드만 식별하여 `imageQuality` 옵션 전달
- ❌ 다른 슬라이드는 기존처럼 `imageQuality` 옵션을 전달하지 않음

**재초담초채권 식별 로직**:
```javascript
const isRechotanchoBond = slide?.mode === 'chart' &&
  (slide?.tab === 'bondChart' || slide?.tab === 'bond') &&
  slide?.subTab === 'rechotanchoBond';
```

#### 2-1. `commonAncestor` 케이스 수정 (1756줄 근처)
```javascript
// 변경 전
blob = await captureElement(commonAncestor, {
  scale: SCALE,
  useCORS: true,
  fixedBottomPaddingPx: 0,
  backgroundColor: '#ffffff',
  scrollX: 0,
  scrollY: 0,
  skipAutoCrop: false,
  height: Math.min(sizeInfo?.measuredHeight || 0, MAX_HEIGHT),
  width: Math.min(sizeInfo?.measuredWidth || 0, MAX_WIDTH),
});

// 변경 후
const captureOptions = {
  scale: SCALE,
  useCORS: true,
  fixedBottomPaddingPx: 0,
  backgroundColor: '#ffffff',
  scrollX: 0,
  scrollY: 0,
  skipAutoCrop: false,
  height: Math.min(sizeInfo?.measuredHeight || 0, MAX_HEIGHT),
  width: Math.min(sizeInfo?.measuredWidth || 0, MAX_WIDTH),
};

// ✅ 재초담초채권만 imageQuality 추가 (다른 슬라이드는 전달하지 않음)
if (isRechotanchoBond && config?.imageQuality) {
  captureOptions.imageQuality = config.imageQuality;
}

blob = await captureElement(commonAncestor, captureOptions);
```

#### 2-2. `composite` 케이스의 헤더/테이블 캡처 (2029줄, 2098줄, 2132줄 근처)
**참고**: 재초담초채권은 `composite` 방식을 사용하지 않지만, 다른 슬라이드와의 일관성을 위해 조건부 처리

```javascript
// 헤더 캡처
const headerCaptureOptions = {
  scale: SCALE,
  useCORS: true,
  fixedBottomPaddingPx: 0,
  backgroundColor: '#ffffff',
  skipAutoCrop: true,
};

// ✅ 재초담초채권만 imageQuality 추가 (실제로는 이 경로를 사용하지 않지만 일관성 유지)
if (isRechotanchoBond && config?.imageQuality) {
  headerCaptureOptions.imageQuality = config.imageQuality;
}

headerBlob = await captureElement(elements.headerElement, headerCaptureOptions);
```

```javascript
// 테이블 캡처
const tableCaptureOptions = {
  scale: SCALE,
  useCORS: true,
  fixedBottomPaddingPx: 0,
  backgroundColor: '#ffffff',
  skipAutoCrop: false,
  width: tableWidth,
  height: tableHeight,
};

// ✅ 재초담초채권만 imageQuality 추가 (실제로는 이 경로를 사용하지 않지만 일관성 유지)
if (isRechotanchoBond && config?.imageQuality) {
  tableCaptureOptions.imageQuality = config.imageQuality;
}

const tableBlob = await captureElement(tableBox, tableCaptureOptions);
```

#### 2-3. `direct` 케이스 수정 (2249줄, 2262줄 근처) - **가장 중요!**
**재초담초채권은 `direct` 방식을 사용하므로 이 부분이 핵심!**

```javascript
// executeCapture 함수 시작 부분에 재초담초채권 식별 추가
async function executeCapture(elements, config, sizeInfo, slide) {
  let blob = null;
  const styleRestores = [];
  
  // ✅ 재초담초채권 슬라이드 식별
  const isRechotanchoBond = slide?.mode === 'chart' &&
    (slide?.tab === 'bondChart' || slide?.tab === 'bond') &&
    slide?.subTab === 'rechotanchoBond';

  try {
    switch (config?.captureMethod) {
      // ... 다른 케이스들 ...
      
      case 'direct':
      default: {
        // ... 기존 코드 ...
        
        if (sizeInfo) {
          // ... 기존 코드 ...
          
          // ✅ 변경: captureOptions 객체 생성 후 조건부 imageQuality 추가
          const directCaptureOptions = {
            scale: SCALE,
            useCORS: true,
            fixedBottomPaddingPx: 0,
            backgroundColor: '#ffffff',
            scrollX: 0,
            scrollY: 0,
            skipAutoCrop: false,
            width: captureWidth,
            height: shouldUseTiledCaptureForTotalClosing ? undefined : captureHeight,
          };
          
          // ✅ 재초담초채권만 imageQuality 추가
          if (isRechotanchoBond && config?.imageQuality) {
            directCaptureOptions.imageQuality = config.imageQuality;
          }
          
          blob = await captureElement(captureElementForDirect, directCaptureOptions);
        } else {
          // sizeInfo가 없는 경우 (기본 캡처)
          const defaultCaptureOptions = {
            scale: SCALE,
            useCORS: true,
            fixedBottomPaddingPx: 0,
            backgroundColor: '#ffffff',
            scrollX: 0,
            scrollY: 0,
            skipAutoCrop: false,
          };
          
          // ✅ 재초담초채권만 imageQuality 추가
          if (isRechotanchoBond && config?.imageQuality) {
            defaultCaptureOptions.imageQuality = config.imageQuality;
          }
          
          blob = await captureElement(captureElementForDirect, defaultCaptureOptions);
        }
        break;
      }
    }
  }
  // ... 나머지 코드 ...
}
```

**검증 포인트**:
- ✅ `isRechotanchoBond` 조건이 정확한지 확인
- ✅ 다른 슬라이드에서는 `imageQuality`가 전달되지 않는지 확인
- ✅ `direct` 케이스가 재초담초채권의 주요 캡처 경로임을 확인

---

### 3단계: screenCapture.js - imageQuality 옵션 처리 (안전하게)

**파일**: `src/utils/screenCapture.js`

**변경 원칙**:
- ✅ `options.imageQuality`가 명시적으로 전달된 경우에만 사용
- ✅ 전달되지 않은 경우 기존 로직 완전히 유지 (변경 없음)

**수정 위치** (972줄 근처):
```javascript
// 재초담초채권 슬라이드 확인
const isRechotanchoBond = slideId.includes('rechotanchoBond');

// ✅ options.imageQuality가 명시적으로 전달된 경우 우선 사용 (재초담초채권)
// 전달되지 않은 경우 기존 로직 유지 (다른 슬라이드)
let quality;
if (options?.imageQuality !== undefined) {
  // UnifiedCaptureEngine에서 명시적으로 전달한 경우 (재초담초채권)
  quality = options.imageQuality;
  if (process.env.NODE_ENV === 'development') {
    console.log(`📦 [screenCapture] 명시적 이미지 품질 사용: ${quality} (slideId: ${slideId})`);
  }
} else {
  // ✅ 기존 로직 완전히 유지 (다른 슬라이드)
  // 목차 슬라이드는 파일 크기가 크므로 더 낮은 품질 사용 (0.80)
  // 재초담초채권 슬라이드는 업로드 안정성을 위해 품질을 낮춤 (0.85)
  quality = isToc ? 0.80 : 
           (isRechotanchoBond ? 0.85 : 
           ((isLargeSlide || isVeryTall) ? 0.90 : 0.95));
  
  if (process.env.NODE_ENV === 'development') {
    if (quality === 0.80) {
      console.log(`📦 [screenCapture] 압축 품질 80% 적용: 목차 슬라이드 (높이: ${estimatedHeight.toFixed(0)}px, 파일 크기 최적화)`);
    } else if (quality === 0.85) {
      console.log(`📦 [screenCapture] 압축 품질 85% 적용: 재초담초채권 슬라이드 (높이: ${estimatedHeight.toFixed(0)}px, 업로드 안정성 향상)`);
    } else if (quality === 0.90) {
      console.log(`📦 [screenCapture] 압축 품질 90% 적용: ${isLargeSlide ? '큰 슬라이드' : '긴 슬라이드'} (높이: ${estimatedHeight.toFixed(0)}px)`);
    }
  }
}
```

**검증 포인트**:
- ✅ `options.imageQuality`가 `undefined`인 경우 기존 로직이 그대로 작동하는지 확인
- ✅ 재초담초채권이 `options.imageQuality`를 받으면 그것을 사용하는지 확인
- ✅ 다른 슬라이드는 기존 로직대로 작동하는지 확인

---

### 4단계: MeetingCaptureManager.js - 업로드 설정 유지

**파일**: `src/components/meeting/MeetingCaptureManager.js`

**현재 상태 확인** (롤백 후):
- 재초담초채권 슬라이드는 이미 특별 처리됨:
  - `uploadTimeout: 60000` (60초)
  - `maxRetries: 7` (7회 재시도)
  - 추가 압축 로직 (1MB 이상 시)

**변경 사항**: **없음 (현재 상태 유지)**

**이유**:
- 재초담초채권 슬라이드는 1.16MB로 크기 때문에 업로드 안정성을 위해 약간의 특별 처리가 필요
- 하지만 이는 업로드 단계에서만이고, 다른 슬라이드에는 영향을 주지 않음

**검증 포인트**:
- ✅ 재초담초채권 슬라이드가 올바르게 식별되는지 확인
- ✅ 업로드 타임아웃과 재시도 설정이 적용되는지 확인

---

## 🔍 검증 계획

### 1. 코드 검증 (수정 후 즉시)
- [ ] 다른 슬라이드 설정에 `imageQuality`가 추가되지 않았는지 확인
- [ ] `UnifiedCaptureEngine`에서 재초담초채권이 아닐 때 `imageQuality`가 전달되지 않는지 확인
- [ ] `screenCapture.js`의 기존 로직이 완전히 유지되는지 확인

### 2. 기능 테스트
- [ ] **재초담초채권 슬라이드**: 이미지 품질 0.85 적용, 업로드 성공 확인
- [ ] **메인 슬라이드**: 기존과 동일하게 작동하는지 확인 (변경 없음)
- [ ] **목차 슬라이드**: 기존과 동일하게 작동하는지 확인 (변경 없음)
- [ ] **엔딩 슬라이드**: 기존과 동일하게 작동하는지 확인 (변경 없음)
- [ ] **가입자증감 슬라이드**: 기존과 동일하게 작동하는지 확인 (변경 없음)
- [ ] **전체총마감 슬라이드**: 기존과 동일하게 작동하는지 확인 (변경 없음)
- [ ] **재고장표 슬라이드**: 기존과 동일하게 작동하는지 확인 (변경 없음)

### 3. 롤백 대비
- 각 단계마다 커밋하여 문제 발생 시 즉시 롤백 가능하도록 함

---

## 📝 실행 순서

1. **1단계 실행**: SlideCaptureConfig.js 수정 → 커밋
2. **2단계 실행**: UnifiedCaptureEngine.js 수정 → 커밋
3. **3단계 실행**: screenCapture.js 수정 → 커밋
4. **4단계 확인**: MeetingCaptureManager.js 확인 (변경 없음)
5. **검증**: 모든 슬라이드 테스트

---

## ⚠️ 주의사항

1. **절대로 다른 슬라이드 설정을 변경하지 않음**
2. **재초담초채권이 아닐 때는 `imageQuality`를 전달하지 않음**
3. **각 단계마다 커밋하여 롤백 가능하도록 함**
4. **기존 로직이 작동하는 다른 슬라이드에 영향을 주지 않도록 보장**
5. **`screenCapture.js`의 기존 로직은 최대한 보존**

---

## 🎯 기대 결과

- ✅ 재초담초채권 슬라이드: 이미지 품질 0.85 적용, 업로드 성공률 향상
- ✅ 다른 모든 슬라이드: 기존과 동일하게 작동 (영향 없음)
- ✅ 코드 유지보수성: 재초담초채권만 별도 처리, 다른 슬라이드와 분리
- ✅ 복잡도 감소: 특별 처리를 최소화하되 필요한 부분만 유지

---

## 📌 핵심 차이점 (이전 플랜 vs 개선 플랜)

| 항목 | 이전 플랜 (문제) | 개선 플랜 (해결) |
|------|-----------------|-----------------|
| SlideCaptureConfig | 모든 슬라이드에 imageQuality 추가 | 재초담초채권만 추가 |
| UnifiedCaptureEngine | 모든 captureElement에 imageQuality 전달 | 재초담초채권일 때만 전달 |
| screenCapture.js | 기존 로직 변경 | 기존 로직 보존, 옵션 있을 때만 사용 |
| MeetingCaptureManager | 특별 처리 완전 제거 | 현재 상태 유지 (안정성 고려) |

